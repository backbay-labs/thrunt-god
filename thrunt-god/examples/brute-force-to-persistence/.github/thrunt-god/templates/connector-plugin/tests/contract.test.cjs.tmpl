'use strict';

/**
 * Contract tests for the {{CONNECTOR_DISPLAY_NAME}} connector
 * Generated by: thrunt-tools init connector-plugin {{CONNECTOR_ID}}
 * Date: {{DATE}}
 *
 * These tests validate that the adapter satisfies the THRUNT GOD connector
 * contract (capabilities shape, method signatures, response normalization).
 */

const { runContractTests } = require('thrunt-god/thrunt-god/bin/lib/contract-tests.cjs');
const { createAdapter } = require('../src/index.cjs');
const { describe } = require('node:test');

describe('{{CONNECTOR_ID}} contract tests', () => {
  runContractTests(() => createAdapter(), { connectorId: '{{CONNECTOR_ID}}' });
});
