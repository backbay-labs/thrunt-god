# {{CONNECTOR_DISPLAY_NAME}} Connector

Generated by: `thrunt-tools init connector {{CONNECTOR_ID}}`
Date: {{DATE}}

## Overview

This connector integrates {{CONNECTOR_DISPLAY_NAME}} with Thrunt's hunt execution engine.
Fill in the 4 marked TODO functions in `thrunt-god/bin/lib/connectors/{{CONNECTOR_ID}}.cjs`
to complete the implementation.

## Supported Capabilities

| Field            | Value                    |
|------------------|--------------------------|
| Connector ID     | `{{CONNECTOR_ID}}`       |
| Auth Types       | {{AUTH_TYPES_ARRAY}}     |
| Dataset Kinds    | {{DATASET_KINDS_ARRAY}}  |
| Query Languages  | {{LANGUAGES_ARRAY}}      |
| Pagination Modes | {{PAGINATION_MODES_ARRAY}}|
| Docs URL         | {{DOCS_URL}}             |

## Fill In These 4 Functions

The generated adapter at `thrunt-god/bin/lib/connectors/{{CONNECTOR_ID}}.cjs` has 4 functions
that need backend-specific implementation:

### 1. `preflight({ profile })`
Validate prerequisites before query execution. Add connector-specific checks such as:
- Required environment variables
- Auth token validity
- API version compatibility

### 2. `prepareQuery({ spec, profile, pagination })`
Translate a QuerySpec into a backend-native HTTP request. Update:
- The API endpoint URL (replace `/api/v1/search` with the real endpoint)
- The request body format (JSON, URL-encoded, GraphQL, etc.)
- Time window mapping to backend query syntax
- Pagination cursor injection

### 3. `normalizeResponse({ response, spec })`
Parse the backend response into the standard envelope shape. Update:
- The `rows` extraction path (replace `response.data?.results`)
- Entity extraction paths (host, user, IP field names)
- Event normalization paths (timestamp, id, title field names)
- Pagination `has_more` and `next_cursor` extraction

### 4. `executeRequest({ prepared, profile, secrets, options })`
Usually requires minimal changes — just verify the `auth.type` matches your connector's
primary auth mechanism (`{{AUTH_TYPES_FIRST}}`).

## Auth Profile Configuration

Configure a connector profile in your `.planning/config.json`:

```json
{
  "connector_profiles": {
    "{{CONNECTOR_ID}}": {
      "production": {
        "auth_type": "{{AUTH_TYPES_FIRST}}",
        "base_url": "https://your-instance.example.com",
        "secret_refs": {
          "access_token": { "type": "env", "value": "{{ENV_PREFIX}}_TOKEN" }
        }
      }
    }
  }
}
```

Set your token:
```bash
export {{ENV_PREFIX}}_TOKEN="your-api-token-here"
```

## Example QuerySpec

```javascript
const result = await runtime.executeQuerySpec({
  connector: { id: '{{CONNECTOR_ID}}', profile: 'production' },
  dataset: { kind: '{{DATASET_KINDS_FIRST}}' },
  time_window: {
    start: '2026-01-01T00:00:00.000Z',
    end: '2026-01-02T00:00:00.000Z',
  },
  query: {
    language: '{{LANGUAGES_FIRST}}',
    statement: '* | head 100',  // Replace with a valid query for {{CONNECTOR_DISPLAY_NAME}}
  },
}, registry, config);
```

## Running Tests

```bash
# Unit tests (uses mock HTTP server, no real connection required)
node --test tests/connectors-{{CONNECTOR_ID}}.test.cjs
```

{{#IF_HAS_DOCKER}}
```bash
# Integration tests (requires Docker)
docker compose -f tests/integration/docker-compose.yml up -d {{CONNECTOR_ID}}
node --test tests/integration/{{CONNECTOR_ID}}.integration.test.cjs
```
{{/IF_HAS_DOCKER}}

## Registration

After filling in the adapter functions, register it in `runtime.cjs`:

```javascript
// In createBuiltInConnectorRegistry():
const { create{{CONNECTOR_FUNCTION_NAME}}Adapter } = require('./connectors/{{CONNECTOR_ID}}.cjs');
// Add create{{CONNECTOR_FUNCTION_NAME}}Adapter() to the registry array
```
