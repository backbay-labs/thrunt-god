# CLAUDE.md Template

Template for project-root `CLAUDE.md` — auto-generated by `thrunt-tools generate-claude-md`.

Contains 6 marker-bounded sections. Each section is independently updatable.
The `generate-claude-md` subcommand manages 5 sections (mission, stack, conventions, architecture, workflow enforcement).
The profile section is managed exclusively by `generate-claude-profile`.

---

## Section Templates

### Mission Section
```
<!-- THRUNT:mission-start source:MISSION.md -->
## Mission

{{mission_content}}
<!-- THRUNT:mission-end -->
```

**Fallback text:**
```
Mission not yet initialized. Run /hunt:new-case or /hunt:new-program to set up.
```

### Stack Section
```
<!-- THRUNT:stack-start source:STACK.md -->
## Technology Stack

{{stack_content}}
<!-- THRUNT:stack-end -->
```

**Fallback text:**
```
Technology stack not yet documented. Will populate after codebase mapping or first phase.
```

### Conventions Section
```
<!-- THRUNT:conventions-start source:CONVENTIONS.md -->
## Conventions

{{conventions_content}}
<!-- THRUNT:conventions-end -->
```

**Fallback text:**
```
Conventions not yet established. Will populate as patterns emerge during development.
```

### Architecture Section
```
<!-- THRUNT:architecture-start source:ARCHITECTURE.md -->
## Architecture

{{architecture_content}}
<!-- THRUNT:architecture-end -->
```

**Fallback text:**
```
Architecture not yet mapped. Follow existing patterns found in the codebase.
```

### Workflow Enforcement Section
```
<!-- THRUNT:workflow-start source:THRUNT defaults -->
## THRUNT Workflow Enforcement

Before using Edit, Write, or other file-changing tools, start work through a THRUNT command so planning artifacts and execution context stay in sync.

Use these entry points:
- `/thrunt:quick` for small fixes, doc updates, and ad-hoc tasks
- `/thrunt:debug` for investigation and bug fixing
- `/hunt:run` for planned phase work

Do not make direct repo edits outside a THRUNT workflow unless the user explicitly asks to bypass it.
<!-- THRUNT:workflow-end -->
```

### Profile Section (Placeholder Only)
```
<!-- THRUNT:profile-start -->
## Developer Profile

> Profile not yet configured. Run `/thrunt:profile-user` to generate your developer profile.
> This section is managed by `generate-claude-profile` — do not edit manually.
<!-- THRUNT:profile-end -->
```

**Note:** This section is NOT managed by `generate-claude-md`. It is managed exclusively
by `generate-claude-profile`. The placeholder above is only used when creating a new
CLAUDE.md file and no profile section exists yet.

---

## Section Ordering

1. **Mission** — Identity and purpose (what this hunt is for)
2. **Stack** — Technology choices (what tools are used)
3. **Conventions** — Code patterns and rules (how code is written)
4. **Architecture** — System structure (how components fit together)
5. **Workflow Enforcement** — Default THRUNT entry points for file-changing work
6. **Profile** — Developer behavioral preferences (how to interact)

## Marker Format

- Start: `<!-- THRUNT:{name}-start source:{file} -->`
- End: `<!-- THRUNT:{name}-end -->`
- Source attribute enables targeted updates when source files change
- Partial match on start marker (without closing `-->`) for detection

## Fallback Behavior

When a source file is missing, fallback text provides Claude-actionable guidance:
- Guides Claude's behavior in the absence of data
- Not placeholder ads or "missing" notices
- Each fallback tells Claude what to do, not just what's absent
