---
phase: 86-live-hunt-canvas-reactive-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/obsidian/src/services/canvas-service.ts
  - apps/obsidian/src/services/intelligence-service.ts
  - apps/obsidian/src/__tests__/live-canvas.test.ts
autonomous: true
requirements: [CANVAS-09, CANVAS-10]

must_haves:
  truths:
    - "handleEntityCreated appends a new file-type node to live-hunt.canvas with correct entity color"
    - "Duplicate entities are not re-added to the live canvas (idempotent by file property)"
    - "Existing node positions (x, y, width, height) are never modified when appending new nodes"
    - "New nodes are positioned in a grid below all existing nodes with 20px gap"
    - "refreshDashboardCanvas patches CANVAS_DASHBOARD.canvas nodes without repositioning"
    - "refreshDashboardCanvas grays out canvas nodes whose entity files no longer exist (color #757575)"
    - "Non-substantive entity changes (whitespace-only) are detected and skipped"
    - "Canvas JSON output uses tab indentation"
    - "Missing or malformed canvas files are silently skipped"
    - "entity:created event is emitted during ingestion for each new entity"
  artifacts:
    - path: "apps/obsidian/src/services/canvas-service.ts"
      provides: "handleEntityCreated, computeNewNodePosition, refreshDashboardCanvas methods"
      contains: "handleEntityCreated"
    - path: "apps/obsidian/src/services/intelligence-service.ts"
      provides: "entity:created EventBus emit in runIngestion"
      contains: "entity:created"
    - path: "apps/obsidian/src/__tests__/live-canvas.test.ts"
      provides: "Unit tests for live canvas and dashboard reactivity"
      contains: "handleEntityCreated"
  key_links:
    - from: "apps/obsidian/src/services/intelligence-service.ts"
      to: "event-bus.ts"
      via: "eventBus.emit('entity:created', ...)"
      pattern: "emit.*entity:created"
    - from: "apps/obsidian/src/services/canvas-service.ts"
      to: "canvas-adapter.ts"
      via: "resolveEntityColor for new node color"
      pattern: "resolveEntityColor"
---

<objective>
Add live canvas auto-population and dashboard reactivity logic to CanvasService, plus entity:created event emission from ingestion.

Purpose: Create the pure testable logic that makes canvas alive during hunts -- new entities auto-append to the live hunt canvas, the knowledge dashboard patches reactively on entity changes, and ingestion emits the events that drive both features.

Output: Extended CanvasService with handleEntityCreated/computeNewNodePosition/refreshDashboardCanvas, IntelligenceService emitting entity:created during ingestion, and comprehensive unit tests.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-live-hunt-canvas-reactive-dashboard/86-CONTEXT.md
@.planning/phases/86-live-hunt-canvas-reactive-dashboard/86-RESEARCH.md
@.planning/phases/85-canvas-adapter-reactive-nodes/85-01-SUMMARY.md
@.planning/phases/85-canvas-adapter-reactive-nodes/85-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/obsidian/src/types.ts:
```typescript
export interface CanvasNode {
  id: string;
  x: number;
  y: number;
  width: number;
  height: number;
  type: 'text' | 'file';
  text?: string;
  file?: string;
  color: string;
}

export interface CanvasEdge {
  id: string;
  fromNode: string;
  toNode: string;
  label?: string;
}

export interface CanvasData {
  nodes: CanvasNode[];
  edges: CanvasEdge[];
}
```

From apps/obsidian/src/services/event-bus.ts:
```typescript
export type EventMap = {
  'cache:invalidated': void;
  'entity:created': { name: string; entityType: string; sourcePath: string };
  'entity:modified': { path: string };
  'ingestion:complete': { created: number; updated: number; skipped: number };
  'canvas:generated': { canvasPath: string };
  'canvas:refreshed': { canvasPath: string; changedCount: number };
};
```

From apps/obsidian/src/canvas-adapter.ts:
```typescript
export function resolveEntityColor(entityType: string): string;
export function patchCanvasNodeColors(canvasData: CanvasData, entityPathToColor: Map<string, string>): { patched: CanvasData; changedCount: number };
export function parseCanvasRelevantFields(content: string): { type: string; verdict: string; confidenceScore: number | undefined };
```

From apps/obsidian/src/services/canvas-service.ts:
```typescript
export class CanvasService {
  constructor(private vaultAdapter: VaultAdapter, private getPlanningDir: () => string, private eventBus?: EventBus);
  async generateHuntCanvas(templateName: ...): Promise<{...}>;
  private isEntityPath(filePath: string): boolean;
  async findCanvasFiles(): Promise<string[]>;
  async handleEntityModified(entityPath: string): Promise<void>;
  async refreshAllCanvasNodes(): Promise<{ totalPatched: number }>;
  async canvasFromCurrentHunt(templateName?: ...): Promise<{...}>;
}
```

From apps/obsidian/src/__tests__/canvas-service-reactive.test.ts (test helpers):
```typescript
function makeEntityContent(type: string): string;
function makeCanvasJson(nodes: CanvasData['nodes'], edges?: CanvasData['edges']): string;
function createMockVault(files?: Record<string, string>): VaultAdapter;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add computeNewNodePosition, handleEntityCreated, refreshDashboardCanvas to CanvasService with tests</name>
  <files>
    apps/obsidian/src/services/canvas-service.ts,
    apps/obsidian/src/__tests__/live-canvas.test.ts
  </files>
  <read_first>
    apps/obsidian/src/services/canvas-service.ts,
    apps/obsidian/src/__tests__/canvas-service-reactive.test.ts,
    apps/obsidian/src/canvas-adapter.ts,
    apps/obsidian/src/services/event-bus.ts,
    apps/obsidian/src/types.ts
  </read_first>
  <behavior>
    computeNewNodePosition tests:
    - Empty canvas returns {x: 0, y: 0} for nodeIndex 0
    - With existing nodes, new row starts at max(y+height) + 20px gap
    - nodeIndex 0-3 fill first row across 4 columns (col * (250 + 20))
    - nodeIndex 4 wraps to second row
    - 4 columns per row, 250px wide nodes, 60px tall, 20px col gap, 20px row gap

    handleEntityCreated tests:
    - Appends new file-type node to existing live-hunt.canvas with resolveEntityColor color
    - Creates live-hunt.canvas with {nodes:[], edges:[]} if it does not exist
    - Does NOT add duplicate node when entity file already present (match by node.file property)
    - Preserves existing node positions (x, y, width, height unchanged)
    - New node has width: 250, height: 60, type: 'file', file: sourcePath
    - Node ID includes timestamp and random suffix (entity-{timestamp}-{random})
    - Canvas JSON written with tab indentation (JSON.stringify(data, null, '\t'))
    - Emits canvas:refreshed event after successful write
    - Silently returns if canvas file exists but is malformed JSON
    - Multiple new entities get sequential grid positions (second entity at col 1, etc.)

    refreshDashboardCanvas tests:
    - Reads CANVAS_DASHBOARD.canvas, rebuilds entity color map, patches colors via patchCanvasNodeColors, preserves positions
    - Returns early if CANVAS_DASHBOARD.canvas does not exist
    - Silently skips malformed CANVAS_DASHBOARD.canvas
    - Emits canvas:refreshed after successful patch
    - Does not write if patchCanvasNodeColors returns changedCount 0
    - Grays out file-type nodes whose entity file no longer exists: sets color to '#757575' per user decision "Mark removed entities visually (gray color) instead"

    isSubstantiveEntityChange tests:
    - Returns true when verdict field changes
    - Returns true when confidence_score changes
    - Returns true when type field changes
    - Returns false when only whitespace or body content changes
  </behavior>
  <action>
    1. Create apps/obsidian/src/__tests__/live-canvas.test.ts with tests described in behavior block.
       Reuse the createMockVault, makeCanvasJson, makeEntityContent helpers from canvas-service-reactive.test.ts (copy them, do not import -- test files should be self-contained).
       Use PLANNING_DIR = 'THRUNT' constant matching existing test pattern.

       IMPORTANT: Include a test for the removed-entity graying behavior:
       - Set up a CANVAS_DASHBOARD.canvas with a file-type node pointing to an entity path (e.g., 'THRUNT/entities/actors/APT28.md')
       - Do NOT include that entity file in the mock vault (simulating a deleted entity)
       - Call refreshDashboardCanvas()
       - Assert the node's color was changed to '#757575' (gray)
       - Assert changedCount includes this gray-out in the canvas:refreshed event

    2. Add to canvas-service.ts:

       a. Export `computeNewNodePosition(existingNodes: CanvasNode[], nodeIndex?: number): { x: number; y: number }` as a standalone exported function (not a method -- pure function for testability):
          - Constants: NODE_WIDTH = 250, NODE_HEIGHT = 60, COL_GAP = 20, ROW_GAP = 20, COLS_PER_ROW = 4
          - If existingNodes is empty: startY = 0
          - Else: startY = max(node.y + node.height for all nodes) + ROW_GAP
          - col = (nodeIndex ?? 0) % COLS_PER_ROW
          - row = Math.floor((nodeIndex ?? 0) / COLS_PER_ROW)
          - Return { x: col * (NODE_WIDTH + COL_GAP), y: startY + row * (NODE_HEIGHT + ROW_GAP) }

       b. Export `isSubstantiveEntityChange(oldContent: string, newContent: string): boolean` as a standalone exported function:
          - Use parseCanvasRelevantFields on both old and new content
          - Return true if type, verdict, or confidenceScore differ
          - Return false otherwise

       c. Add method `handleEntityCreated(event: { name: string; entityType: string; sourcePath: string }): Promise<void>` to CanvasService class:
          - Get planningDir, build canvasPath = normalizePath(`${planningDir}/live-hunt.canvas`)
          - If file exists: try JSON.parse, catch -> return silently (malformed)
          - If file does not exist: canvasData = { nodes: [], edges: [] }
          - Check if canvasData.nodes has any node with type === 'file' && file === event.sourcePath. If found, return (idempotent).
          - Count how many new nodes are being added in this batch (filter existing file nodes from live-hunt.canvas, count total file nodes). Use nodeIndex = number of existing file-type nodes minus the count of nodes that were already there. SIMPLER: just use the total existing node count as nodeIndex for the single new node being appended.
          - Actually, since each call adds exactly one node, use nodeIndex = 0 for position calculation (the computeNewNodePosition already uses existing nodes to find the bottom). The nodeIndex parameter is for batch adds. For single adds, pass nodeIndex = count of nodes already at the same startY row.
          - SIMPLEST APPROACH: compute { x, y } = computeNewNodePosition(canvasData.nodes, 0). But this always puts new nodes at col 0. For proper grid behavior, track how many nodes are at the bottom row. Count nodes where node.y === max bottom y. Use that count as nodeIndex.
          - ACTUALLY, use the approach from RESEARCH.md: computeNewNodePosition(canvasData.nodes, 0). Each invocation reads the current canvas state, so the "bottom row count" is naturally handled because the previous node's y+height shifts maxBottom. Since nodes are 60px tall and row gap is 20px, a node at col 0 row N has y = startY. The NEXT node also computes startY from the same maxBottom (since the first new node hasn't been written yet... but it HAS because we write after each entity:created). So sequential entity:created events will each read the updated canvas and get the right position. For batch (multiple entities in one event), we'd need nodeIndex. But since entity:created fires per entity and we write each time, nodeIndex=0 works. However, to fill columns across a row, we need to detect existing nodes at the bottom row. Let's do: find all nodes where node.y >= maxBottom - NODE_HEIGHT (i.e., on the "last row"). Count them. Use that count as nodeIndex to fill across columns.
          - Compute position: find maxBottom = max(n.y + n.height) for all nodes, or 0 if empty. Find nodesOnLastRow = nodes where n.y >= maxBottom - NODE_HEIGHT. Use nodesOnLastRow.length as nodeIndex. Call computeNewNodePosition(canvasData.nodes, nodesOnLastRow.length).
          - WAIT -- this is overcomplicating it. The computeNewNodePosition function takes existingNodes and computes startY from them. If nodeIndex > 0, it adjusts col/row. For the single-add case: each time handleEntityCreated is called, it reads the canvas, gets all existing nodes, and needs to figure out WHERE in the new row this entity goes. Since we write to disk after each add, the next call reads the updated canvas. So the sequence is: first entity -> reads empty canvas -> nodeIndex=0 -> writes node at (0, 0). Second entity -> reads canvas with 1 node -> maxBottom = 60, startY = 80 -> nodeIndex=0 -> writes at (0, 80). This puts each entity in a new row! That's not grid behavior. We want them across columns first.
          - CORRECT APPROACH: Don't use computeNewNodePosition for single adds at all. Instead, find the bottom-most row of nodes. Count how many nodes are in that row. If count < COLS_PER_ROW, place in the same row at next col. If count >= COLS_PER_ROW, start new row.
          - Let me reconsider computeNewNodePosition: it should work for BATCH scenarios where you know the index within the batch. For SINGLE adds from handleEntityCreated, inline the logic:
            1. If no nodes exist: x=0, y=0
            2. Find maxBottom = max(n.y + n.height) across all nodes
            3. Find all nodes on the bottom row: nodes where n.y >= maxBottom - NODE_HEIGHT
            4. bottomRowCount = those nodes length
            5. If bottomRowCount < COLS_PER_ROW: same row, next col -> x = bottomRowCount * (NODE_WIDTH + COL_GAP), y = those nodes[0].y
            6. If bottomRowCount >= COLS_PER_ROW: new row -> x = 0, y = maxBottom + ROW_GAP

          - Create new node: { id: `entity-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`, x, y, width: 250, height: 60, type: 'file', file: event.sourcePath, color: resolveEntityColor(event.entityType) }
          - Push to canvasData.nodes
          - Serialize with JSON.stringify(canvasData, null, '\t')
          - If file existed: modifyFile. If not: createFile.
          - Emit canvas:refreshed with canvasPath and changedCount: 1

       d. Add method `refreshDashboardCanvas(): Promise<void>` to CanvasService class:
          - Build canvasPath = normalizePath(`${planningDir}/CANVAS_DASHBOARD.canvas`) -- use CANVAS_DASHBOARD.canvas (the ACTUAL path from intelligence-service.ts line 479), NOT knowledge-dashboard.canvas from CONTEXT.md
          - If file does not exist: return early
          - Try JSON.parse, catch -> return silently
          - Build entity color map: scan all entity folders (reuse pattern from refreshAllCanvasNodes), parse each entity file for type, resolve color
          - GRAY OUT REMOVED ENTITIES (per user decision "Never remove nodes -- mark removed entities visually (gray color) instead"):
            After building the entityPathToColor map, iterate all file-type nodes in the canvas.
            For each file-type node whose `file` path starts with the entities directory prefix
            AND whose `file` path is NOT present in the entityPathToColor map (meaning the entity file no longer exists on disk),
            add that node's file path to entityPathToColor with value '#757575' (FALLBACK_COLOR / gray).
            This ensures patchCanvasNodeColors will gray out orphaned entity nodes without removing them from the canvas.
          - Call patchCanvasNodeColors(canvasData, entityPathToColor)
          - If changedCount === 0: return (no write needed)
          - Write patched data with tab indentation
          - Emit canvas:refreshed

    3. Run tests: cd apps/obsidian && npx vitest run src/__tests__/live-canvas.test.ts
    4. Run full suite: cd apps/obsidian && npx vitest run
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/thrunt-god-worktree/apps/obsidian && npx vitest run src/__tests__/live-canvas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "computeNewNodePosition" apps/obsidian/src/services/canvas-service.ts
    - grep -q "handleEntityCreated" apps/obsidian/src/services/canvas-service.ts
    - grep -q "refreshDashboardCanvas" apps/obsidian/src/services/canvas-service.ts
    - grep -q "isSubstantiveEntityChange" apps/obsidian/src/services/canvas-service.ts
    - grep -q "live-hunt.canvas" apps/obsidian/src/services/canvas-service.ts
    - grep -q "CANVAS_DASHBOARD.canvas" apps/obsidian/src/services/canvas-service.ts
    - grep -q "resolveEntityColor" apps/obsidian/src/services/canvas-service.ts
    - grep -q "#757575" apps/obsidian/src/services/canvas-service.ts  (gray-out for removed entities)
    - grep -q "handleEntityCreated" apps/obsidian/src/__tests__/live-canvas.test.ts
    - grep -q "refreshDashboardCanvas" apps/obsidian/src/__tests__/live-canvas.test.ts
    - grep -q "computeNewNodePosition" apps/obsidian/src/__tests__/live-canvas.test.ts
    - grep -q "isSubstantiveEntityChange" apps/obsidian/src/__tests__/live-canvas.test.ts
    - grep -q "gray\|removed\|no longer exist\|#757575" apps/obsidian/src/__tests__/live-canvas.test.ts  (removed entity gray-out test)
  </acceptance_criteria>
  <done>
    computeNewNodePosition places nodes in 4-column grid below existing nodes.
    handleEntityCreated appends idempotent file-type nodes to live-hunt.canvas with entity color.
    refreshDashboardCanvas patches CANVAS_DASHBOARD.canvas colors preserving positions.
    refreshDashboardCanvas grays out (#757575) canvas nodes whose entity files no longer exist on disk.
    isSubstantiveEntityChange detects verdict/confidence/type changes.
    All tests pass including full suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emit entity:created event from IntelligenceService.runIngestion()</name>
  <files>
    apps/obsidian/src/services/intelligence-service.ts,
    apps/obsidian/src/__tests__/live-canvas.test.ts
  </files>
  <read_first>
    apps/obsidian/src/services/intelligence-service.ts,
    apps/obsidian/src/services/event-bus.ts,
    apps/obsidian/src/__tests__/live-canvas.test.ts
  </read_first>
  <action>
    1. In intelligence-service.ts runIngestion() method, after the line `await this.vaultAdapter.createFile(entityNotePath, content);` and the `created++;` line (around line 163-164), add:
       ```typescript
       // Emit entity:created for live canvas auto-population
       this.eventBus?.emit('entity:created', {
         name: instruction.name,
         entityType: instruction.entityType,
         sourcePath: entityNotePath,
       });
       ```
       This goes ONLY in the "else" branch where a new entity note is created (not the "update sighting" branch).

    2. Also emit 'ingestion:complete' after the return statement block. Actually, check if ingestion:complete is already emitted. Looking at the code, after building the IngestionResult and appending to INGESTION_LOG.md, the method returns the result. Add before the return:
       ```typescript
       this.eventBus?.emit('ingestion:complete', { created, updated, skipped });
       ```
       (Only add if not already present.)

    3. Add a test to live-canvas.test.ts verifying that entity:created is emitted during ingestion:
       - Create an IntelligenceService instance with a mock vault containing a RECEIPTS/RCT-test.md file
       - Attach a spy to EventBus.on('entity:created', ...)
       - Call runIngestion()
       - Assert the spy was called with the expected entity name, type, and sourcePath
       - This test may be complex due to ingestion dependencies. If too complex, create a simpler test that directly verifies the emit call exists in the code path by checking eventBus.emit is called with 'entity:created' using a partial mock of IntelligenceService. SIMPLEST: just test that EventBus.emit is called by directly calling runIngestion on a minimal IntelligenceService with a receipt file.

    4. Run: cd apps/obsidian && npx vitest run src/__tests__/live-canvas.test.ts
    5. Run full suite: cd apps/obsidian && npx vitest run
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/thrunt-god-worktree/apps/obsidian && npx vitest run src/__tests__/live-canvas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "entity:created" apps/obsidian/src/services/intelligence-service.ts
    - grep -q "emit.*entity:created" apps/obsidian/src/services/intelligence-service.ts
    - grep -q "ingestion:complete" apps/obsidian/src/services/intelligence-service.ts
    - grep -q "emitted\|entity:created" apps/obsidian/src/__tests__/live-canvas.test.ts
  </acceptance_criteria>
  <done>
    IntelligenceService.runIngestion() emits entity:created for each new entity file created.
    IntelligenceService.runIngestion() emits ingestion:complete with counts.
    Test verifies the emit call fires during ingestion.
    Full test suite passes.
  </done>
</task>

</tasks>

<verification>
- `cd apps/obsidian && npx vitest run src/__tests__/live-canvas.test.ts` -- all live canvas tests pass
- `cd apps/obsidian && npx vitest run` -- full suite passes with zero regressions
- grep confirms handleEntityCreated, refreshDashboardCanvas, computeNewNodePosition, isSubstantiveEntityChange exist in canvas-service.ts
- grep confirms entity:created emit exists in intelligence-service.ts
- grep confirms #757575 gray-out logic in canvas-service.ts for removed entities
</verification>

<success_criteria>
- CanvasService has handleEntityCreated that appends nodes to live-hunt.canvas with grid positioning and idempotent dedup
- CanvasService has refreshDashboardCanvas that patches CANVAS_DASHBOARD.canvas colors preserving analyst layout
- refreshDashboardCanvas grays out nodes whose entity files no longer exist on disk (#757575) per user decision
- computeNewNodePosition pure function places nodes in 4-column grid below existing content
- isSubstantiveEntityChange detects verdict/confidence/type changes for filtering dashboard updates
- IntelligenceService.runIngestion() emits entity:created for each new entity
- All tests pass including new live-canvas tests and full regression suite
</success_criteria>

<output>
After completion, create `.planning/phases/86-live-hunt-canvas-reactive-dashboard/86-01-SUMMARY.md`
</output>
