---
phase: 73-mcp-enrichment-intelligence-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/obsidian/src/types.ts
  - apps/obsidian/src/mcp-enrichment.ts
  - apps/obsidian/src/__tests__/mcp-enrichment.test.ts
autonomous: true
requirements: [MCP-03, MCP-04, MCP-05]

must_haves:
  truths:
    - "mergeEnrichment appends an ## MCP Enrichment section to a TTP note without overwriting analyst content"
    - "buildCoverageReport produces a markdown table with per-tactic coverage percentages and gap rows"
    - "formatDecisionEntry appends to a TTP note ## Decisions section with timestamp and rationale"
    - "formatLearningEntry produces a log entry for LEARNINGS.md with topic and learning text"
  artifacts:
    - path: "apps/obsidian/src/mcp-enrichment.ts"
      provides: "Pure enrichment/coverage/logging functions"
      exports: ["mergeEnrichment", "buildCoverageReport", "formatDecisionEntry", "formatLearningEntry"]
    - path: "apps/obsidian/src/types.ts"
      provides: "EnrichmentData, CoverageReport, CoverageTactic, SearchResult types"
    - path: "apps/obsidian/src/__tests__/mcp-enrichment.test.ts"
      provides: "Unit tests for all pure functions"
  key_links:
    - from: "apps/obsidian/src/mcp-enrichment.ts"
      to: "apps/obsidian/src/types.ts"
      via: "import EnrichmentData, CoverageReport"
      pattern: "import.*EnrichmentData.*CoverageReport"
---

<objective>
Create pure data-processing functions for MCP enrichment, coverage analysis, and decision/learning logging. These are zero-Obsidian-import modules that accept data and return markdown strings, following the same pure-module pattern as ingestion.ts.

Purpose: Separate testable business logic from Obsidian wiring so Plan 02 can focus on commands and modal without testing data transformations.
Output: mcp-enrichment.ts with 4 exported functions, types in types.ts, comprehensive unit tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-mcp-client-adapter-connection-infrastructure/72-01-SUMMARY.md
@.planning/phases/72-mcp-client-adapter-connection-infrastructure/72-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/obsidian/src/mcp-client.ts:
```typescript
export interface McpClient {
  getStatus(): McpConnectionStatus;
  isConnected(): boolean;
  connect(): Promise<void>;
  disconnect(): void;
  checkHealth(): Promise<McpHealthResponse | null>;
  callTool(name: string, args: Record<string, unknown>): Promise<McpToolResult | null>;
}
```

From apps/obsidian/src/types.ts:
```typescript
export interface McpToolResult {
  content: Array<{ type: string; text: string }>;
  isError?: boolean;
}
```

From apps/obsidian/src/entity-schema.ts:
```typescript
// TTP entity type uses folder 'entities/ttps', frontmatter has mitre_id, tactic, platforms, data_sources, hunt_count
```

From apps/obsidian/src/ingestion.ts (pattern to follow):
```typescript
// Pure module: zero Obsidian imports, functions accept data and return data
// Vault I/O wired in workspace.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MCP enrichment types to types.ts</name>
  <files>apps/obsidian/src/types.ts</files>
  <read_first>
    - apps/obsidian/src/types.ts
  </read_first>
  <behavior>
    - EnrichmentData has fields: description (string), groups (string[]), detectionSources (string[]), relatedTechniques (string[])
    - CoverageTactic has fields: tactic (string), total (number), hunted (number), percentage (number)
    - CoverageReport has fields: tactics (CoverageTactic[]), totalTechniques (number), huntedTechniques (number), overallPercentage (number), gaps (string[])
    - SearchResult has fields: id (string), name (string), entityType (string), snippet (string)
  </behavior>
  <action>
    Append 4 new interfaces to the end of types.ts (before the closing line if any):

    1. `EnrichmentData` -- shape of parsed MCP lookupTechnique response:
       - `description: string` -- technique description text
       - `groups: string[]` -- associated threat actor groups
       - `detectionSources: string[]` -- data sources that detect this technique
       - `relatedTechniques: string[]` -- related technique IDs (e.g. T1059.001)

    2. `CoverageTactic` -- one row in the coverage report:
       - `tactic: string` -- tactic name (e.g. "Initial Access")
       - `total: number` -- total techniques in this tactic
       - `hunted: number` -- techniques with hunt_count > 0
       - `percentage: number` -- hunted/total * 100, rounded to 1 decimal

    3. `CoverageReport` -- full coverage analysis output:
       - `tactics: CoverageTactic[]`
       - `totalTechniques: number`
       - `huntedTechniques: number`
       - `overallPercentage: number`
       - `gaps: string[]` -- technique IDs with hunt_count === 0

    4. `SearchResult` -- one result from knowledge graph search:
       - `id: string` -- entity identifier
       - `name: string` -- display name
       - `entityType: string` -- e.g. 'ttp', 'ioc/ip', 'actor'
       - `snippet: string` -- context snippet from search hit

    No imports needed -- these are plain interfaces.
  </action>
  <acceptance_criteria>
    - grep "export interface EnrichmentData" apps/obsidian/src/types.ts returns 1 match
    - grep "export interface CoverageReport" apps/obsidian/src/types.ts returns 1 match
    - grep "export interface CoverageTactic" apps/obsidian/src/types.ts returns 1 match
    - grep "export interface SearchResult" apps/obsidian/src/types.ts returns 1 match
    - grep "description: string" apps/obsidian/src/types.ts returns at least 1 match (from EnrichmentData)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/obsidian && npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <done>4 new interfaces exported from types.ts, TypeScript compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create mcp-enrichment.ts pure module with TDD</name>
  <files>apps/obsidian/src/mcp-enrichment.ts, apps/obsidian/src/__tests__/mcp-enrichment.test.ts</files>
  <read_first>
    - apps/obsidian/src/types.ts
    - apps/obsidian/src/ingestion.ts
    - apps/obsidian/src/__tests__/ingestion.test.ts
  </read_first>
  <behavior>
    mergeEnrichment(existingContent: string, data: EnrichmentData): string
    - Appends "## MCP Enrichment" section after last existing heading if no such section exists
    - If "## MCP Enrichment" already exists, replaces its content (between ## MCP Enrichment and next ## or EOF)
    - Section contains: "**Description:** {data.description}", "**Groups:** {comma-joined}", "**Detection Sources:** {comma-joined}", "**Related Techniques:** {wiki-linked list}"
    - Never modifies content above ## MCP Enrichment
    - Handles empty data fields gracefully (shows "None" or empty)
    - Test: mergeEnrichment with no existing section appends it
    - Test: mergeEnrichment with existing section replaces only that section
    - Test: analyst content above ## MCP Enrichment is preserved

    buildCoverageReport(tactics: CoverageTactic[], totalTechniques: number, huntedTechniques: number, overallPercentage: number, gaps: string[]): string
    - Returns markdown string with "# Detection Coverage Report" heading
    - Contains a markdown table: | Tactic | Total | Hunted | Coverage |
    - Each CoverageTactic row formatted as: | {tactic} | {total} | {hunted} | {percentage}% |
    - Summary line: "**Overall: {huntedTechniques}/{totalTechniques} ({overallPercentage}%)**"
    - Gaps section: "## Detection Gaps" with bulleted list of gap technique IDs as wiki-links
    - Generated timestamp line at bottom
    - Test: buildCoverageReport with 2 tactics produces correct table rows
    - Test: buildCoverageReport with 0 gaps shows "No detection gaps identified."

    formatDecisionEntry(techniqueId: string, decision: string, rationale: string): string
    - Returns a markdown block: "### {ISO date} - {techniqueId}" + "**Decision:** {decision}" + "**Rationale:** {rationale}" + blank line
    - Test: formatDecisionEntry produces expected markdown with timestamp

    formatLearningEntry(topic: string, learning: string): string
    - Returns a markdown block: "### {ISO date} - {topic}" + "{learning}" + blank line
    - Test: formatLearningEntry produces expected markdown with timestamp
  </behavior>
  <action>
    **RED phase:** Create apps/obsidian/src/__tests__/mcp-enrichment.test.ts with tests for all 4 functions. Tests should import from '../mcp-enrichment'. Run tests -- all must fail (module does not exist yet). Commit with message starting "test(73-01):".

    **GREEN phase:** Create apps/obsidian/src/mcp-enrichment.ts as a pure module (zero Obsidian imports, following ingestion.ts pattern).

    Functions to implement:

    1. `mergeEnrichment(existingContent: string, data: EnrichmentData): string`
       - Check if `## MCP Enrichment` heading exists in existingContent
       - If exists: find the range from `## MCP Enrichment` to the next `## ` heading (or EOF) and replace
       - If not: append `\n\n## MCP Enrichment\n\n` at end of content
       - Build enrichment block:
         ```
         **Description:** {data.description || "No description available."}

         **Groups:** {data.groups.length > 0 ? data.groups.join(', ') : "None"}

         **Detection Sources:** {data.detectionSources.length > 0 ? data.detectionSources.join(', ') : "None"}

         **Related Techniques:** {data.relatedTechniques.length > 0 ? data.relatedTechniques.map(t => `[[${t}]]`).join(', ') : "None"}
         ```
       - Append enrichment block after the heading

    2. `buildCoverageReport(tactics: CoverageTactic[], totalTechniques: number, huntedTechniques: number, overallPercentage: number, gaps: string[]): string`
       - Start with `# Detection Coverage Report\n\n`
       - Add `_Generated: {ISO timestamp}_\n\n`
       - Add markdown table header: `| Tactic | Total | Hunted | Coverage |\n|--------|-------|--------|----------|\n`
       - Add one row per tactic: `| {tactic} | {total} | {hunted} | {percentage}% |\n`
       - Add blank line + `**Overall: {huntedTechniques}/{totalTechniques} ({overallPercentage}%)**\n\n`
       - Add `## Detection Gaps\n\n`
       - If gaps.length > 0: add bullet list `- [[{gapId}]]\n` for each gap
       - If gaps.length === 0: add `No detection gaps identified.\n`

    3. `formatDecisionEntry(techniqueId: string, decision: string, rationale: string): string`
       - Return: `### {new Date().toISOString().slice(0, 10)} - {techniqueId}\n\n**Decision:** {decision}\n\n**Rationale:** {rationale}\n`

    4. `formatLearningEntry(topic: string, learning: string): string`
       - Return: `### {new Date().toISOString().slice(0, 10)} - {topic}\n\n{learning}\n`

    Run tests -- all must pass. Commit with message starting "feat(73-01):".
  </action>
  <acceptance_criteria>
    - grep "export function mergeEnrichment" apps/obsidian/src/mcp-enrichment.ts returns 1 match
    - grep "export function buildCoverageReport" apps/obsidian/src/mcp-enrichment.ts returns 1 match
    - grep "export function formatDecisionEntry" apps/obsidian/src/mcp-enrichment.ts returns 1 match
    - grep "export function formatLearningEntry" apps/obsidian/src/mcp-enrichment.ts returns 1 match
    - grep "## MCP Enrichment" apps/obsidian/src/__tests__/mcp-enrichment.test.ts returns at least 1 match
    - grep "buildCoverageReport" apps/obsidian/src/__tests__/mcp-enrichment.test.ts returns at least 1 match
    - cd apps/obsidian && npx vitest run --reporter=verbose 2>&1 | grep -c "PASS" returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd apps/obsidian && npx vitest run src/__tests__/mcp-enrichment.test.ts --reporter=verbose</automated>
  </verify>
  <done>All mcp-enrichment pure functions pass unit tests, zero Obsidian imports in module, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `cd apps/obsidian && npx tsc --noEmit --skipLibCheck` -- TypeScript compiles with new types and module
2. `cd apps/obsidian && npx vitest run` -- All tests pass (existing + new mcp-enrichment tests)
3. `grep -c "import.*obsidian" apps/obsidian/src/mcp-enrichment.ts` -- Returns 0 (pure module, no Obsidian imports)
</verification>

<success_criteria>
- 4 new types exported from types.ts (EnrichmentData, CoverageTactic, CoverageReport, SearchResult)
- mcp-enrichment.ts exports 4 pure functions with zero Obsidian imports
- Unit tests cover: enrichment merge (new section, existing section, content preservation), coverage report (table format, gaps, no-gaps), decision entry, learning entry
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/73-mcp-enrichment-intelligence-features/73-01-SUMMARY.md`
</output>
