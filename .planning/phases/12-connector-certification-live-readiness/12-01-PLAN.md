---
phase: 12-connector-certification-live-readiness
plan: 01
type: execute
wave: 1
depends_on:
  - "11"
files_modified:
  - thrunt-god/bin/lib/runtime.cjs
  - thrunt-god/bin/lib/commands.cjs
  - thrunt-god/bin/thrunt-tools.cjs
  - docs/COMMANDS.md
  - docs/CONFIGURATION.md
  - docs/ARCHITECTURE.md
  - docs/FEATURES.md
  - commands/hunt/run.md
  - tests/runtime-contract.test.cjs
  - tests/runtime-doctor.test.cjs
  - tests/docs-contract.test.cjs
autonomous: false
requirements:
  - HYP-01
user_setup: []
must_haves:
  truths:
    - "Operators can assess connector readiness before running live hunts"
    - "Connectors can run a live read-only smoke test without normal hunt artifact emission"
    - "Connectors without a shipped smoke query can define a profile-local smoke test"
  artifacts:
    - path: "tests/runtime-doctor.test.cjs"
      provides: "Command-level coverage for runtime doctor and runtime smoke"
    - path: "docs/CONFIGURATION.md"
      provides: "Profile-level smoke_test contract and operator usage"
  key_links:
    - from: "thrunt-god/bin/lib/commands.cjs"
      to: "thrunt-god/bin/lib/runtime.cjs"
      via: "runtime doctor/smoke route through readiness scoring and live smoke execution"
---

<objective>
Build a connector-certification layer that proves runtime readiness before evidence workflows depend on it.

Purpose: give operators a safe, explicit way to validate auth, preflight requirements, and live smoke queries for real connectors.
Output: readiness scoring in the runtime, `runtime doctor`, `runtime smoke`, profile-defined `smoke_test` support, updated docs, and regression coverage.
</objective>

<execution_context>
@~/.claude/thrunt-god/workflows/execute-plan.md
@~/.claude/thrunt-god/templates/summary.md
</execution_context>

<context>
@.planning/HUNTMAP.md
@.planning/phases/12-connector-certification-live-readiness/12-CONTEXT.md
@thrunt-god/bin/lib/runtime.cjs
@thrunt-god/bin/lib/commands.cjs
@thrunt-god/bin/thrunt-tools.cjs
@docs/CONFIGURATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add readiness scoring and smoke-spec resolution to the runtime</name>
  <files>thrunt-god/bin/lib/runtime.cjs, tests/runtime-contract.test.cjs</files>
  <read_first>.planning/phases/12-connector-certification-live-readiness/12-CONTEXT.md, thrunt-god/bin/lib/runtime.cjs</read_first>
  <action>Build per-connector readiness assessment, built-in and profile-defined smoke-spec resolution, and the live-smoke execution path on top of the shared runtime.</action>
  <verify>node --test tests/runtime-contract.test.cjs</verify>
  <acceptance_criteria>
    - Readiness scoring reflects adapter presence, profile validity, auth readiness, preflight, smoke spec, and optional live smoke.
    - Profile-defined `smoke_test` config works for connectors without shipped safe smoke queries.
  </acceptance_criteria>
  <done>The runtime can certify connectors instead of merely executing them.</done>
</task>

<task type="auto">
  <name>Task 2: Expose operator-facing certification commands</name>
  <files>thrunt-god/bin/lib/commands.cjs, thrunt-god/bin/thrunt-tools.cjs, tests/runtime-doctor.test.cjs</files>
  <read_first>thrunt-god/bin/lib/commands.cjs, thrunt-god/bin/thrunt-tools.cjs</read_first>
  <action>Add `runtime doctor` and `runtime smoke` so operators can score readiness and run live read-only smoke tests from the CLI.</action>
  <verify>node --test tests/runtime-doctor.test.cjs</verify>
  <acceptance_criteria>
    - `runtime doctor` reports per-connector readiness and can opt into `--live`.
    - `runtime smoke` executes the active smoke spec for one or more configured connectors.
  </acceptance_criteria>
  <done>Connector certification is available from the supported CLI surface.</done>
</task>

<task type="auto">
  <name>Task 3: Document and lock the certification contract</name>
  <files>docs/COMMANDS.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md, docs/FEATURES.md, commands/hunt/run.md, tests/docs-contract.test.cjs</files>
  <read_first>docs/COMMANDS.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md</read_first>
  <action>Document readiness scoring, smoke-test configuration, and operator workflow, then add public-doc contract tests so the surface cannot drift silently.</action>
  <verify>node --test tests/docs-contract.test.cjs</verify>
  <acceptance_criteria>
    - Public docs explain `runtime doctor`, `runtime smoke`, and profile `smoke_test`.
    - Hunt-run docs point operators at the certification flow when onboarding or debugging connectors.
  </acceptance_criteria>
  <done>The certification layer is discoverable and protected by regression tests.</done>
</task>

</tasks>

<verification>
- [x] `node --test tests/runtime-contract.test.cjs`
- [x] `node --test tests/runtime-doctor.test.cjs`
- [x] `node --test tests/docs-contract.test.cjs`
- [x] `npm test`
- [x] `node thrunt-god/bin/thrunt-tools.cjs validate health`
</verification>

<success_criteria>

- Operators can assess connector readiness before trusting live hunt results
- Live smoke tests cover both built-in safe connector probes and profile-defined smoke queries
- Certification docs and tests protect the public/runtime contract from drift
</success_criteria>

<output>
After completion, create `.planning/phases/12-connector-certification-live-readiness/12-01-SUMMARY.md`
</output>
