---
phase: 12-shared-design-system-webview-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - thrunt-god-vscode/webview/drain-template-viewer/styles.css
  - thrunt-god-vscode/webview/drain-template-viewer/index.tsx
  - thrunt-god-vscode/webview/drain-template-viewer/app.tsx
  - thrunt-god-vscode/src/drainViewer.ts
autonomous: true
requirements: [DSYS-05, DSYS-06]

must_haves:
  truths:
    - "Drain Template Viewer renders identically after migration from --viewer-* to --hunt-* tokens"
    - "Drain Template Viewer imports tokens.css from webview/shared/ instead of defining its own token variables"
    - "Keyboard navigation via roving tabindex works on the cluster chip list and pinned template list"
    - "ARIA roles are applied to interactive lists in the drain viewer"
  artifacts:
    - path: "thrunt-god-vscode/webview/drain-template-viewer/styles.css"
      provides: "Drain viewer styles using --hunt-* tokens (no --viewer-* references)"
      contains: "--hunt-"
    - path: "thrunt-god-vscode/webview/drain-template-viewer/index.tsx"
      provides: "Entry point importing shared tokens and using shared hooks"
      contains: "import.*tokens.css"
    - path: "thrunt-god-vscode/webview/drain-template-viewer/app.tsx"
      provides: "Drain viewer app using shared components where applicable"
      contains: "useRovingTabindex"
  key_links:
    - from: "webview/drain-template-viewer/styles.css"
      to: "webview/shared/tokens.css"
      via: "CSS custom property references"
      pattern: "var\\(--hunt-"
    - from: "webview/drain-template-viewer/index.tsx"
      to: "webview/shared/tokens.css"
      via: "CSS import"
      pattern: "import.*shared/tokens"
    - from: "webview/drain-template-viewer/app.tsx"
      to: "webview/shared/hooks/"
      via: "hook import"
      pattern: "import.*useRovingTabindex"
---

<objective>
Migrate the existing Drain Template Viewer from `--viewer-*` CSS tokens to the shared `--hunt-*` token system, and wire keyboard navigation (roving tabindex) into the viewer's interactive lists.

Purpose: Validate the shared design system with a known-working surface before new surfaces are built in Phases 13-15. Ensures the token rename produces identical rendering and the keyboard navigation hook works in a real webview.

Output: Drain viewer consuming shared tokens.css, no `--viewer-*` references remaining, roving tabindex on cluster chips and pinned templates.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-shared-design-system-webview-infrastructure/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (tokens and hooks the viewer will consume) -->

From webview/shared/tokens.css:
```css
:root {
  --hunt-bg: var(--vscode-editor-background);
  --hunt-surface: color-mix(...);
  --hunt-panel-border: color-mix(...);
  --hunt-text: var(--vscode-editor-foreground);
  --hunt-text-muted: color-mix(...);
  --hunt-accent: var(--vscode-textLink-foreground);
  --hunt-accent-strong: color-mix(...);
  --hunt-danger: var(--vscode-charts-red, #d16969);
  --hunt-shadow: 0 24px 60px rgba(0, 0, 0, 0.18);
}
```

From webview/shared/hooks/index.ts:
```ts
export { useRovingTabindex } from './useRovingTabindex';
export { useTheme } from './useTheme';
export { useVsCodeApi, createVsCodeApi } from './useVsCodeApi';
export { useHostMessage } from './useHostMessage';
```

From webview/shared/hooks/useRovingTabindex.ts:
```ts
export function useRovingTabindex(
  containerRef: preact.RefObject<HTMLElement>,
  itemSelector: string
): void
```

From src/drainViewer.ts:
```ts
export function createDrainViewerHtml(
  webview: vscode.Webview,
  extensionUri: vscode.Uri,
  bootData: DrainViewerBootData
): string
// References 'dist/webview-drain.js' and 'dist/webview-drain.css'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Drain Template Viewer from --viewer-* to --hunt-* tokens</name>
  <files>
    thrunt-god-vscode/webview/drain-template-viewer/styles.css
    thrunt-god-vscode/webview/drain-template-viewer/index.tsx
    thrunt-god-vscode/src/drainViewer.ts
  </files>
  <read_first>
    thrunt-god-vscode/webview/drain-template-viewer/styles.css
    thrunt-god-vscode/webview/drain-template-viewer/index.tsx
    thrunt-god-vscode/webview/drain-template-viewer/app.tsx
    thrunt-god-vscode/webview/shared/tokens.css
    thrunt-god-vscode/src/drainViewer.ts
  </read_first>
  <action>
**styles.css migration:**

1. Remove the entire `:root { ... }` block (lines 1-12) that defines `--viewer-*` tokens. These are now in `webview/shared/tokens.css` as `--hunt-*`.

2. Remove the `body[data-theme='light']` and `body[data-theme='dark']` blocks (lines 20-29) that override `--viewer-*` tokens. These are now in `webview/shared/tokens.css`.

3. Remove the `body { ... }` background gradient block (lines 14-18). Move this to the end of the file as a drain-viewer-specific body style that references `--hunt-*` tokens:
```css
body {
  background:
    radial-gradient(circle at top right, color-mix(in srgb, var(--hunt-accent) 18%, transparent), transparent 30%),
    linear-gradient(180deg, color-mix(in srgb, var(--hunt-bg) 90%, black 10%), var(--hunt-bg));
}
```

4. Find-and-replace all remaining `--viewer-` references to `--hunt-`:
   - `var(--viewer-bg)` -> `var(--hunt-bg)`
   - `var(--viewer-surface)` -> `var(--hunt-surface)`
   - `var(--viewer-surface-strong)` -> `var(--hunt-surface-strong)`
   - `var(--viewer-panel-border)` -> `var(--hunt-panel-border)`
   - `var(--viewer-text)` -> `var(--hunt-text)`
   - `var(--viewer-text-muted)` -> `var(--hunt-text-muted)`
   - `var(--viewer-accent)` -> `var(--hunt-accent)`
   - `var(--viewer-accent-strong)` -> `var(--hunt-accent-strong)`
   - `var(--viewer-danger)` -> `var(--hunt-danger)`
   - `var(--viewer-shadow)` -> `var(--hunt-shadow)`

5. Remove the `* { box-sizing: border-box }` and `button { font: inherit }` resets (lines 31-37) -- these are now in tokens.css.

6. Keep all component-specific class rules (`.viewer-shell`, `.hero-panel`, `.stat-card`, `.chart-panel`, etc.) and the media queries. These stay in the drain viewer's own stylesheet since they are drain-viewer-specific layout, not shared components. The existing drain viewer continues to use its own class names (`.stat-card`, `.ghost-button`, etc.) -- it does NOT switch to the new `.hunt-stat-card` / `.hunt-ghost-button` classes yet. The shared components are for new surfaces. The drain viewer just needs the token rename.

**index.tsx changes:**

1. Add `import '../shared/tokens.css';` BEFORE `import './styles.css';` so shared tokens are loaded first, then drain-viewer-specific overrides layer on top.

2. Refactor to use `useTheme` hook from shared hooks instead of manual `document.body.dataset.theme` manipulation. Replace the isDark/setIsDark state and the theme useEffect (lines 97-99) with:
```tsx
import { useTheme } from '../shared/hooks';
// ...
const { isDark, setIsDark } = useTheme();
```

3. Optionally refactor to use `useHostMessage` for the message listener. However, the current message handler has complex state dependencies (viewModel, selectedTemplateId). Keep the existing manual message handler for now -- don't force-fit the hook where it adds complexity. The hook is designed for simpler use cases in new surfaces.

**src/drainViewer.ts changes:**

1. Update `createDrainViewerHtml` to also reference the shared tokens CSS file. Add a second `<link>` tag for the shared tokens:
```ts
const tokensUri = webview.asWebviewUri(
  vscode.Uri.joinPath(extensionUri, 'dist', 'webview-drain.css')
);
```
Actually, since esbuild bundles CSS imports into the output CSS file, the `import '../shared/tokens.css'` in index.tsx will cause esbuild to concatenate shared tokens into `dist/webview-drain.css`. So no change needed in drainViewer.ts -- the existing single CSS link will contain both shared tokens and drain-viewer-specific styles after the build.

Verify this by running `npm run build` and checking that `dist/webview-drain.css` contains `--hunt-bg`.

2. Update the `BASE_WEBVIEW_STYLES` inline style: no changes needed (it only references `--vscode-*` variables directly, not `--viewer-*`).

3. In app.tsx, update the two inline references to `--viewer-accent-strong` and `--viewer-panel-border` in the chart rect stroke attributes (lines 211-217):
   - `'var(--viewer-accent-strong)'` -> `'var(--hunt-accent-strong)'`
   - `'var(--viewer-panel-border)'` -> `'var(--hunt-panel-border)'`
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/thrunt-god-drain/thrunt-god-vscode && npm run build 2>&1 | tail -10 && grep -c '\-\-viewer-' webview/drain-template-viewer/styles.css webview/drain-template-viewer/app.tsx webview/drain-template-viewer/index.tsx 2>/dev/null; grep -c '\-\-hunt-' dist/webview-drain.css 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `webview/drain-template-viewer/styles.css` contains zero occurrences of `--viewer-` (grep returns 0)
    - `webview/drain-template-viewer/styles.css` contains `var(--hunt-bg)`, `var(--hunt-surface)`, `var(--hunt-accent)`, `var(--hunt-panel-border)`, `var(--hunt-text)`, `var(--hunt-text-muted)`
    - `webview/drain-template-viewer/styles.css` does NOT contain a `:root {` block defining custom properties (token definitions live in tokens.css)
    - `webview/drain-template-viewer/index.tsx` contains `import '../shared/tokens.css'`
    - `webview/drain-template-viewer/index.tsx` contains `import { useTheme } from '../shared/hooks'` or equivalent
    - `webview/drain-template-viewer/app.tsx` contains `var(--hunt-accent-strong)` (not `--viewer-accent-strong`)
    - `webview/drain-template-viewer/app.tsx` contains `var(--hunt-panel-border)` (not `--viewer-panel-border`)
    - `dist/webview-drain.css` contains `--hunt-bg` (tokens bundled into output CSS)
    - `npm run build` exits 0
    - `npm run test:unit` passes (existing tests still work)
  </acceptance_criteria>
  <done>Drain Template Viewer styles fully migrated from --viewer-* to --hunt-* tokens. Shared tokens.css imported in index.tsx. Build produces dist/webview-drain.css containing both shared tokens and viewer-specific styles. All existing unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire roving tabindex keyboard navigation into Drain Template Viewer</name>
  <files>
    thrunt-god-vscode/webview/drain-template-viewer/app.tsx
  </files>
  <read_first>
    thrunt-god-vscode/webview/drain-template-viewer/app.tsx
    thrunt-god-vscode/webview/shared/hooks/useRovingTabindex.ts
    thrunt-god-vscode/webview/shared/hooks/index.ts
  </read_first>
  <action>
Add roving tabindex keyboard navigation to the two interactive lists in the Drain Template Viewer:

1. **Cluster chip list** (the `.cluster-list` grid of template buttons in `DrainChart` component):
   - Add a `useRef<HTMLDivElement>` for the cluster list container
   - Attach the ref to the `<div class="cluster-list" role="list">` element
   - Call `useRovingTabindex(clusterListRef, '.cluster-chip')` to enable arrow key navigation between cluster chips
   - Add `role="listitem"` to each `<button class="cluster-chip">` element (currently has no role)

2. **Pinned template list** (the `.pinned-list` grid in `PinnedTemplates` component):
   - Add a `useRef<HTMLDivElement>` for the pinned list container
   - Attach the ref to the `<div class="pinned-list" role="list">` element
   - Call `useRovingTabindex(pinnedListRef, '.pinned-chip')` to enable arrow key navigation between pinned chips
   - Add `role="listitem"` to each `<button class="pinned-chip">` element

3. **Import statement**: Add at top of app.tsx:
```tsx
import { useRovingTabindex } from '../shared/hooks';
```

4. **ARIA landmark roles**: Ensure each major section has an appropriate ARIA role:
   - The `.viewer-shell` main element already uses `<main>` (good)
   - Add `aria-label="Template clusters"` to the cluster list container
   - Add `aria-label="Pinned templates"` to the pinned list container

Do NOT change the chart SVG rect keyboard handling (lines 196-199 in app.tsx) -- that already has its own tabindex and keydown handling via direct DOM manipulation on the Observable Plot output. The roving tabindex hook is for the Preact-rendered lists only.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/thrunt-god-drain/thrunt-god-vscode && npx tsc --noEmit 2>&1 | head -5 && npm run build 2>&1 | tail -5 && npm run test:unit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `webview/drain-template-viewer/app.tsx` contains `import { useRovingTabindex } from '../shared/hooks'` or equivalent import
    - `webview/drain-template-viewer/app.tsx` contains two calls to `useRovingTabindex` (one for cluster list, one for pinned list)
    - `webview/drain-template-viewer/app.tsx` contains `aria-label="Template clusters"` on the cluster list container
    - `webview/drain-template-viewer/app.tsx` contains `aria-label="Pinned templates"` on the pinned list container
    - `webview/drain-template-viewer/app.tsx` contains `role="listitem"` on cluster chip buttons
    - `webview/drain-template-viewer/app.tsx` contains `role="listitem"` on pinned chip buttons
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - `npm run test:unit` passes
  </acceptance_criteria>
  <done>Roving tabindex keyboard navigation works on both the cluster chip list and pinned template list in the Drain Template Viewer. Arrow keys, Home, and End navigate between items. ARIA roles and labels applied to interactive lists. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds -- drain viewer builds with shared token imports
2. `npm run test:unit` passes -- no regressions from token migration
3. Zero occurrences of `--viewer-` in drain viewer source files
4. `dist/webview-drain.css` contains `--hunt-bg` (shared tokens bundled)
5. `useRovingTabindex` called on both interactive lists in app.tsx
6. ARIA roles applied to cluster and pinned template lists
</verification>

<success_criteria>
- Drain Template Viewer renders identically after --viewer-* to --hunt-* migration (visual parity maintained by keeping same CSS values under new names)
- Keyboard navigation with arrow keys works on cluster chip and pinned template lists
- No --viewer-* token references remain anywhere in the drain viewer
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-shared-design-system-webview-infrastructure/12-02-SUMMARY.md`
</output>
