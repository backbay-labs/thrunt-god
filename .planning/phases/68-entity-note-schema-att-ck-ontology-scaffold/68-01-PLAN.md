---
phase: 68-entity-note-schema-att-ck-ontology-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/obsidian/src/entity-schema.ts
  - apps/obsidian/src/types.ts
  - apps/obsidian/src/paths.ts
  - apps/obsidian/src/__tests__/entity-schema.test.ts
autonomous: true
requirements:
  - ONTO-02

must_haves:
  truths:
    - "8 entity types are defined with canonical folder paths and typed frontmatter schemas"
    - "IOC subtypes (ip, domain, hash) share a single folder but differ by frontmatter type field"
    - "Entity starter templates produce valid YAML frontmatter with snake_case field names"
    - "Path resolution functions exist for entity folders under planningDir"
  artifacts:
    - path: "apps/obsidian/src/entity-schema.ts"
      provides: "ENTITY_TYPES registry and ENTITY_FOLDERS constant"
      exports: ["ENTITY_TYPES", "ENTITY_FOLDERS", "EntityTypeDefinition", "FrontmatterFieldDef"]
    - path: "apps/obsidian/src/types.ts"
      provides: "EntityTypeDefinition and FrontmatterFieldDef type exports"
      contains: "EntityTypeDefinition"
    - path: "apps/obsidian/src/paths.ts"
      provides: "getEntityFolder function"
      exports: ["getEntityFolder"]
    - path: "apps/obsidian/src/__tests__/entity-schema.test.ts"
      provides: "Registry validation and template generation tests"
      min_lines: 60
  key_links:
    - from: "apps/obsidian/src/entity-schema.ts"
      to: "apps/obsidian/src/types.ts"
      via: "import EntityTypeDefinition"
      pattern: "import.*EntityTypeDefinition.*from.*types"
    - from: "apps/obsidian/src/entity-schema.ts"
      to: "MILESTONES-v2.md section 3.1"
      via: "frontmatter field definitions match spec"
      pattern: "type.*ioc/ip|type.*ttp|type.*actor"
---

<objective>
Create the entity type registry that defines all 8 entity types with their canonical folders, frontmatter schemas, and starter templates. This is the foundation module that both the ATT&CK scaffold (Plan 02) and bootstrap extension (Plan 03) depend on.

Purpose: Establishes the knowledge schema contract -- every downstream module (scaffold, bootstrap, ingestion, sidebar) references this registry to know what entity types exist, where they live, and what their frontmatter looks like.
Output: `entity-schema.ts` pure data module, extended `types.ts` and `paths.ts`, and comprehensive tests.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-entity-note-schema-att-ck-ontology-scaffold/68-CONTEXT.md
@.planning/phases/68-entity-note-schema-att-ck-ontology-scaffold/68-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/obsidian/src/types.ts:
```typescript
export type WorkspaceStatus = 'healthy' | 'partial' | 'missing';

export interface ArtifactDefinition {
  fileName: string;
  label: string;
  description: string;
  starterTemplate: string;
  commandId: string;
  commandName: string;
}
```

From apps/obsidian/src/paths.ts:
```typescript
export function normalizePath(path: string): string;
export function getPlanningDir(configuredDir: string, defaultDir: string): string;
export function getCoreFilePath(planningDir: string, fileName: string): string;
```

From apps/obsidian/src/artifacts.ts (pattern to mirror):
```typescript
export const CORE_ARTIFACTS: readonly ArtifactDefinition[] = Object.freeze([
  { fileName: 'MISSION.md', label: 'Mission', ... },
  // ... 5 entries
]);
```

From apps/obsidian/MILESTONES-v2.md section 3.1 (entity frontmatter spec):
| Entity Type | Folder | Key Frontmatter Fields |
|-------------|--------|----------------------|
| IOC (IP) | entities/iocs/ | type: ioc/ip, value, first_seen, last_seen, hunt_refs[], confidence, verdict |
| IOC (Domain) | entities/iocs/ | type: ioc/domain, value, first_seen, last_seen, hunt_refs[], confidence, verdict |
| IOC (Hash) | entities/iocs/ | type: ioc/hash, hash_type, value, first_seen, hunt_refs[], confidence |
| TTP | entities/ttps/ | type: ttp, mitre_id, tactic, platforms[], data_sources[], hunt_count, last_hunted |
| Actor | entities/actors/ | type: actor, aliases[], mitre_group_id, associated_ttps[], hunt_refs[] |
| Tool / Malware | entities/tools/ | type: tool, category, associated_actors[], associated_ttps[], hunt_refs[] |
| Infrastructure | entities/infra/ | type: infrastructure, kind (C2/proxy/hosting), associated_actors[], ioc_refs[] |
| Data Source | entities/datasources/ | type: datasource, platform, retention, coverage_ttps[] |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define entity type registry and path helpers</name>
  <files>apps/obsidian/src/entity-schema.ts, apps/obsidian/src/types.ts, apps/obsidian/src/paths.ts, apps/obsidian/src/__tests__/entity-schema.test.ts</files>
  <read_first>
    - apps/obsidian/src/types.ts
    - apps/obsidian/src/paths.ts
    - apps/obsidian/src/artifacts.ts
    - apps/obsidian/src/__tests__/artifacts.test.ts
    - apps/obsidian/MILESTONES-v2.md (lines 50-65 for entity frontmatter spec)
  </read_first>
  <behavior>
    - ENTITY_TYPES has exactly 8 entries covering types: ioc/ip, ioc/domain, ioc/hash, ttp, actor, tool, infrastructure, datasource
    - All IOC types (ioc/ip, ioc/domain, ioc/hash) have folder "entities/iocs"
    - Each non-IOC type has a distinct folder (entities/ttps, entities/actors, entities/tools, entities/infra, entities/datasources)
    - ENTITY_FOLDERS has exactly 6 entries (unique folders)
    - Every entity type has a non-empty starterTemplate function that returns a string containing valid YAML frontmatter
    - IOC/IP template contains: type: ioc/ip, value, first_seen, last_seen, hunt_refs, confidence, verdict
    - IOC/Domain template contains: type: ioc/domain, value, first_seen, last_seen, hunt_refs, confidence, verdict
    - IOC/Hash template contains: type: ioc/hash, hash_type, value, first_seen, hunt_refs, confidence
    - TTP template contains: type: ttp, mitre_id, tactic, platforms, data_sources, hunt_count, last_hunted
    - Actor template contains: type: actor, aliases, mitre_group_id, associated_ttps, hunt_refs
    - Tool template contains: type: tool, category, associated_actors, associated_ttps, hunt_refs
    - Infrastructure template contains: type: infrastructure, kind, associated_actors, ioc_refs
    - Data Source template contains: type: datasource, platform, retention, coverage_ttps
    - Every template includes ## Sightings and ## Related sections
    - All frontmatter field keys use snake_case
    - getEntityFolder("planning", "iocs") returns "planning/entities/iocs"
  </behavior>
  <action>
1. Add types to `apps/obsidian/src/types.ts` -- append these interfaces AFTER the existing `WorkspaceError` class at the bottom of the file:

```typescript
// --- Entity schema types ---

export interface FrontmatterFieldDef {
  key: string;
  type: 'string' | 'number' | 'string[]' | 'date';
  default: string | number | string[] | null;
  required: boolean;
}

export interface EntityTypeDefinition {
  type: string;           // "ioc/ip", "ioc/domain", "ioc/hash", "ttp", "actor", "tool", "infrastructure", "datasource"
  label: string;          // Human-readable label
  folder: string;         // Relative folder under planningDir: "entities/iocs", "entities/ttps", etc.
  frontmatterFields: FrontmatterFieldDef[];
  starterTemplate: (name: string) => string;
}
```

2. Add path helper to `apps/obsidian/src/paths.ts` -- append this function AFTER the existing `getCoreFilePath`:

```typescript
export function getEntityFolder(
  planningDir: string,
  entityFolder: string,
): string {
  return normalizePath(`${planningDir}/${entityFolder}`);
}
```

3. Create `apps/obsidian/src/entity-schema.ts` -- a pure data module (NO Obsidian imports) containing:

- Import `EntityTypeDefinition` from `./types`
- Define `ENTITY_TYPES: readonly EntityTypeDefinition[]` with Object.freeze(), containing exactly 8 entries:

  a) **ioc/ip**: folder "entities/iocs", fields: type (string, default "ioc/ip", required), value (string, required), first_seen (date), last_seen (date), hunt_refs (string[]), confidence (string), verdict (string). Template produces frontmatter with those fields and ## Sightings + ## Related sections.

  b) **ioc/domain**: folder "entities/iocs", same fields as ioc/ip except type default is "ioc/domain".

  c) **ioc/hash**: folder "entities/iocs", fields: type (string, default "ioc/hash", required), hash_type (string, default "sha256"), value (string, required), first_seen (date), hunt_refs (string[]), confidence (string). No last_seen or verdict compared to IP/domain.

  d) **ttp**: folder "entities/ttps", fields: type (string, default "ttp", required), mitre_id (string, required), tactic (string), platforms (string[]), data_sources (string[]), hunt_count (number, default 0), last_hunted (date).

  e) **actor**: folder "entities/actors", fields: type (string, default "actor", required), aliases (string[]), mitre_group_id (string), associated_ttps (string[]), hunt_refs (string[]).

  f) **tool**: folder "entities/tools", fields: type (string, default "tool", required), category (string), associated_actors (string[]), associated_ttps (string[]), hunt_refs (string[]).

  g) **infrastructure**: folder "entities/infra", fields: type (string, default "infrastructure", required), kind (string -- C2/proxy/hosting), associated_actors (string[]), ioc_refs (string[]).

  h) **datasource**: folder "entities/datasources", fields: type (string, default "datasource", required), platform (string), retention (string), coverage_ttps (string[]).

- Define `ENTITY_FOLDERS: readonly string[]` with Object.freeze() -- the 6 unique folder paths: "entities/iocs", "entities/ttps", "entities/actors", "entities/tools", "entities/infra", "entities/datasources".

- Each starterTemplate function accepts a `name: string` parameter and returns markdown with:
  - YAML frontmatter block (--- delimited) with all fields set to their defaults
  - `# ${name}` heading
  - `## Sightings` section with `_No sightings recorded yet._`
  - `## Related` section (empty, ready for wiki-links)

- All frontmatter keys MUST be snake_case (e.g., `first_seen`, `hunt_refs`, `mitre_id`, `hunt_count`, `last_hunted`, `hash_type`, `mitre_group_id`, `associated_ttps`, `associated_actors`, `ioc_refs`, `coverage_ttps`).

4. Create `apps/obsidian/src/__tests__/entity-schema.test.ts` with tests for:
  - ENTITY_TYPES has exactly 8 entries
  - ENTITY_FOLDERS has exactly 6 entries
  - All 8 expected type strings are present
  - IOC types share folder "entities/iocs"
  - Non-IOC types each have distinct folders
  - Every type has non-empty label and folder
  - Every type's starterTemplate("test") returns a string starting with "---"
  - IOC/IP template contains "type: ioc/ip" and "value:" and "first_seen:" and "## Sightings" and "## Related"
  - TTP template contains "type: ttp" and "mitre_id:" and "tactic:" and "hunt_count: 0"
  - Actor template contains "type: actor" and "aliases:" and "mitre_group_id:"
  - All templates use snake_case field names (no camelCase like firstName, lastName)
  - getEntityFolder tests: ("planning", "entities/iocs") returns "planning/entities/iocs"
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/thrunt-god-worktree/apps/obsidian && npx vitest run src/__tests__/entity-schema.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - apps/obsidian/src/entity-schema.ts contains `export const ENTITY_TYPES: readonly EntityTypeDefinition[]`
    - apps/obsidian/src/entity-schema.ts contains `export const ENTITY_FOLDERS: readonly string[]`
    - apps/obsidian/src/entity-schema.ts does NOT contain `import` from `obsidian` (pure data module)
    - apps/obsidian/src/types.ts contains `export interface EntityTypeDefinition`
    - apps/obsidian/src/types.ts contains `export interface FrontmatterFieldDef`
    - apps/obsidian/src/paths.ts contains `export function getEntityFolder(`
    - apps/obsidian/src/__tests__/entity-schema.test.ts exits 0
    - grep for all 8 type strings in entity-schema.ts: "ioc/ip", "ioc/domain", "ioc/hash", "ttp", "actor", "tool", "infrastructure", "datasource"
    - grep for all 6 folder paths: "entities/iocs", "entities/ttps", "entities/actors", "entities/tools", "entities/infra", "entities/datasources"
  </acceptance_criteria>
  <done>ENTITY_TYPES registry has 8 entries with correct frontmatter schemas matching MILESTONES-v2.md spec, ENTITY_FOLDERS has 6 unique paths, path helper resolves entity folders under planningDir, all tests green</done>
</task>

</tasks>

<verification>
- `cd apps/obsidian && npx vitest run` -- all tests pass (existing + new entity-schema tests)
- `cd apps/obsidian && npx tsc --noEmit --skipLibCheck` -- no type errors
- grep confirms ENTITY_TYPES has 8 entries and ENTITY_FOLDERS has 6 entries
</verification>

<success_criteria>
- entity-schema.ts is a pure data module with no Obsidian imports
- 8 entity types defined with correct folders and frontmatter per MILESTONES-v2.md section 3.1
- IOC subtypes share entities/iocs/ folder, differentiated by type field
- All frontmatter uses snake_case
- getEntityFolder resolves paths under planningDir
- Tests cover registry shape, template content, and path resolution
</success_criteria>

<output>
After completion, create `.planning/phases/68-entity-note-schema-att-ck-ontology-scaffold/68-01-SUMMARY.md`
</output>
