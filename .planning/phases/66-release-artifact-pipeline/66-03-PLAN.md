---
phase: 66-release-artifact-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["66-01", "66-02"]
files_modified:
  - docs/obsidian-release.md
  - tests/obsidian-release.test.cjs
  - tests/release-workflow.test.cjs
autonomous: true
requirements:
  - RELEASE-01
  - RELEASE-02
  - RELEASE-03
  - RELEASE-04
must_haves:
  truths:
    - "Release contract tests assert the Obsidian dependency install step, version-alignment guards, bundle build step, and GitHub release uploads"
    - "A targeted Obsidian release test exercises the shared bundle script, verifies the output directory contains the expected four files, and proves the shared version contract throws on drift"
    - "Maintainers have a repo-local runbook that documents the local bundle command, the version-sync rules, and the expected GitHub release assets"
    - "Targeted release tests run green locally"
  artifacts:
    - path: "tests/release-workflow.test.cjs"
      provides: "Workflow-level release contract coverage including Obsidian steps"
    - path: "tests/obsidian-release.test.cjs"
      provides: "Bundle-script output and version contract coverage"
    - path: "docs/obsidian-release.md"
      provides: "Maintainer release runbook for Obsidian assets"
  key_links:
    - from: "tests/obsidian-release.test.cjs"
      to: "scripts/build-obsidian-release.cjs"
      via: "bundle contract verification"
      pattern: "build-obsidian-release"
    - from: "docs/obsidian-release.md"
      to: ".github/workflows/release.yml"
      via: "maintainer runbook mirrors the CI release flow"
      pattern: "Release"
---

<objective>
Lock down the Obsidian release pipeline with regression coverage and a maintainer-facing runbook.

Purpose: This plan makes the release workflow durable for future changes. After it lands, the repo has explicit tests for the new Obsidian release steps plus a concise maintainer document describing how local bundle generation and CI release wiring fit together.

Output: Expanded release contract tests, a new Obsidian release bundle test, and a maintainer-facing release runbook.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-release-artifact-pipeline/66-CONTEXT.md
@.github/workflows/release.yml
@tests/release-workflow.test.cjs
@scripts/build-obsidian-release.cjs
@scripts/lib/obsidian-artifacts.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend workflow-level release contract tests for the new Obsidian steps</name>
  <files>tests/release-workflow.test.cjs</files>
  <read_first>
    - tests/release-workflow.test.cjs
    - .github/workflows/release.yml
  </read_first>
  <action>
Update `tests/release-workflow.test.cjs` so it asserts the Obsidian release pipeline contract:

1. Keep the existing release-workflow assertions intact.
2. Add assertions for:
   - `apps/obsidian/package-lock.json` in `cache-dependency-path`
   - `npm --prefix apps/obsidian ci`
   - Obsidian version-alignment guards referencing `apps/obsidian/package.json`, `apps/obsidian/manifest.json`, and `apps/obsidian/versions.json`
   - `npm run bundle:obsidian-release`
   - `dist/obsidian-release/main.js`
   - `dist/obsidian-release/manifest.json`
   - `dist/obsidian-release/styles.css`
   - `dist/obsidian-release/versions.json`
3. Assert the release title is no longer VS Code-specific.
  </action>
  <verify>
    <automated>node --test tests/release-workflow.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/release-workflow.test.cjs` covers Obsidian dependency install, version guards, bundle build, and release upload paths
    - `node --test tests/release-workflow.test.cjs` exits 0
  </acceptance_criteria>
  <done>The release workflow contract test now enforces the new Obsidian release behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add an Obsidian bundle-script regression test that verifies local release outputs</name>
  <files>tests/obsidian-release.test.cjs</files>
  <read_first>
    - scripts/build-obsidian-release.cjs
    - scripts/lib/obsidian-artifacts.cjs
    - apps/obsidian/package.json
    - apps/obsidian/manifest.json
    - apps/obsidian/versions.json
  </read_first>
  <action>
Create `tests/obsidian-release.test.cjs` with targeted node:test coverage for the local release bundle command:

1. Execute `node scripts/build-obsidian-release.cjs`.
2. Parse its JSON output and assert the output directory exists.
3. Assert the output directory contains:
   - `main.js`
   - `manifest.json`
   - `styles.css`
   - `versions.json`
4. Assert `versions.json` in the output directory still maps the root/root-Obsidian version to `manifest.minAppVersion`.
5. Add one focused contract assertion for `assertObsidianVersionSync(...)` that proves a mismatched root/package/manifest/versions input throws instead of silently succeeding.
6. Keep the test isolated to the release bundle output and clean up any temp artifacts it creates.
  </action>
  <verify>
    <automated>node --test tests/obsidian-release.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/obsidian-release.test.cjs` exists and exercises `scripts/build-obsidian-release.cjs`
    - the test asserts the release bundle contains the expected four assets
    - the test asserts the output `versions.json` mapping is still valid
    - the test asserts the shared version contract throws on drift
    - `node --test tests/obsidian-release.test.cjs` exits 0
  </acceptance_criteria>
  <done>The local Obsidian release bundle contract is covered by a focused regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Write a maintainer runbook for the Obsidian release flow and run the targeted release checks</name>
  <files>docs/obsidian-release.md, tests/release-workflow.test.cjs, tests/obsidian-release.test.cjs</files>
  <read_first>
    - .github/workflows/release.yml
    - scripts/build-obsidian-release.cjs
    - tests/release-workflow.test.cjs
    - tests/obsidian-release.test.cjs
  </read_first>
  <action>
Create `docs/obsidian-release.md` with a concise maintainer runbook:

1. Document the local command:
   - `npm run bundle:obsidian-release`
2. Document the version-sync contract:
   - root `package.json`
   - `apps/obsidian/package.json`
   - `apps/obsidian/manifest.json`
   - `apps/obsidian/versions.json`
3. Document the expected GitHub release assets:
   - `main.js`
   - `manifest.json`
   - `styles.css`
   - `versions.json`
4. Document the CI path:
   - `release.yml` installs Obsidian deps
   - `release.yml` runs `npm run bundle:obsidian-release`
   - GitHub release upload attaches the four Obsidian assets
5. Run the targeted checks together:
   - `node --test tests/release-workflow.test.cjs tests/obsidian-release.test.cjs`
  </action>
  <verify>
    <automated>node --test tests/release-workflow.test.cjs tests/obsidian-release.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `docs/obsidian-release.md` documents the local bundle command, version-sync rules, and four GitHub release assets
    - `docs/obsidian-release.md` references the CI release workflow path
    - the combined targeted release tests exit 0
  </acceptance_criteria>
  <done>The Obsidian release pipeline has both regression coverage and a maintainer-facing runbook.</done>
</task>

</tasks>

<verification>
- `node --test tests/release-workflow.test.cjs tests/obsidian-release.test.cjs` exits 0
- `docs/obsidian-release.md` exists and lists the four expected release assets
- `node scripts/build-obsidian-release.cjs` emits the four expected bundle files
</verification>

<success_criteria>
The Obsidian release pipeline is validated locally, enforced by regression tests, and documented for maintainers.
</success_criteria>

<output>
After completion, create `.planning/phases/66-release-artifact-pipeline/66-03-SUMMARY.md`
</output>
