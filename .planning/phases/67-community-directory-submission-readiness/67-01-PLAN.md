---
phase: 67-community-directory-submission-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/obsidian/.gitignore
  - apps/obsidian/src/artifacts.ts
  - apps/obsidian/src/main.ts
  - apps/obsidian/src/settings.ts
  - tests/obsidian-community-review.test.cjs
autonomous: true
requirements:
  - COMM-01
must_haves:
  truths:
    - "The Obsidian package closes the concrete community-review gaps already identified in context: redundant plugin-name command labels, single-section settings heading misuse, and accidental build-output tracking risk"
    - "The plugin still satisfies the mobile-safe manifest baseline and keeps theme-safe styling scoped to plugin classes"
    - "Review-critical package rules are covered by a targeted regression test instead of relying on memory"
  artifacts:
    - path: "apps/obsidian/src/main.ts"
      provides: "Community-review-safe command labels and plugin UX copy"
    - path: "apps/obsidian/src/artifacts.ts"
      provides: "Community-review-safe artifact command labels from the canonical registry"
    - path: "apps/obsidian/src/settings.ts"
      provides: "Settings UI that follows current Obsidian checklist guidance"
    - path: "apps/obsidian/.gitignore"
      provides: "Guardrail against accidentally committing `main.js` build output"
    - path: "tests/obsidian-community-review.test.cjs"
      provides: "Static contract checks for current community-review expectations"
  key_links:
    - from: "tests/obsidian-community-review.test.cjs"
      to: "apps/obsidian/src/main.ts"
      via: "command-label review assertions"
      pattern: "addCommand"
    - from: "tests/obsidian-community-review.test.cjs"
      to: "apps/obsidian/src/settings.ts"
      via: "settings-header review assertions"
      pattern: "setHeading"
---

<objective>
Close the concrete Obsidian plugin review gaps already visible in the package and lock them down with regression coverage.

Purpose: This plan makes the plugin package itself low-friction for community review. After it lands, the current checklist blockers are fixed in code and enforced by a small repo-level review contract test.

Output: Updated Obsidian plugin source files plus a targeted community-review regression test.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-community-directory-submission-readiness/67-CONTEXT.md
@apps/obsidian/src/main.ts
@apps/obsidian/src/settings.ts
@apps/obsidian/manifest.json
@apps/obsidian/styles.css
@apps/obsidian/.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the concrete plugin-package review gaps in commands, settings UI, and build-output hygiene</name>
  <files>apps/obsidian/.gitignore, apps/obsidian/src/artifacts.ts, apps/obsidian/src/main.ts, apps/obsidian/src/settings.ts</files>
  <read_first>
    - apps/obsidian/src/artifacts.ts
    - apps/obsidian/src/main.ts
    - apps/obsidian/src/settings.ts
    - apps/obsidian/manifest.json
    - apps/obsidian/styles.css
    - .planning/phases/67-community-directory-submission-readiness/67-CONTEXT.md
  </read_first>
  <action>
Apply the current Obsidian checklist guidance to the package:

1. In `apps/obsidian/src/artifacts.ts` and `apps/obsidian/src/main.ts`, remove redundant plugin-name phrasing from command labels while preserving the plugin's intent and clarity.
2. In `apps/obsidian/src/settings.ts`, remove or replace the single-section `h2` heading so the settings tab follows current Obsidian UI guidance.
3. Keep `apps/obsidian/manifest.json` mobile-safe (`isDesktopOnly` remains `false`) and avoid introducing desktop-only assumptions while making these compliance fixes.
4. Track `apps/obsidian/.gitignore` so `main.js` remains an ignored build artifact and cannot be accidentally committed during future release prep.
  </action>
  <verify>
    <automated>npm --prefix apps/obsidian run build</automated>
  </verify>
  <acceptance_criteria>
    - Obsidian command labels no longer redundantly repeat the plugin name
    - the settings tab no longer uses a raw single-section `h2` heading
    - `apps/obsidian/.gitignore` is tracked and ignores `main.js`
    - `npm --prefix apps/obsidian run build` exits 0
  </acceptance_criteria>
  <done>The plugin package no longer contains the obvious current-review issues surfaced during context gathering.</done>
</task>

<task type="auto">
  <name>Task 2: Add a targeted community-review contract test for package-level readiness rules</name>
  <files>tests/obsidian-community-review.test.cjs</files>
  <read_first>
    - apps/obsidian/src/main.ts
    - apps/obsidian/src/settings.ts
    - apps/obsidian/manifest.json
    - apps/obsidian/styles.css
  </read_first>
  <action>
Create `tests/obsidian-community-review.test.cjs` as a focused static review contract:

1. Assert the manifest still declares `isDesktopOnly: false`.
2. Assert `apps/obsidian/src/main.ts` no longer contains the current redundant plugin-name command labels.
3. Assert `apps/obsidian/src/settings.ts` no longer creates a raw `h2` heading for the single settings section.
4. Assert `apps/obsidian/styles.css` continues to rely on scoped plugin classes and Obsidian CSS variables rather than hardcoded theme overrides.
  </action>
  <verify>
    <automated>node --test tests/obsidian-community-review.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/obsidian-community-review.test.cjs` exists and enforces the package-level review rules
    - `node --test tests/obsidian-community-review.test.cjs` exits 0
    - the review contract test fails if the current command-name or settings-heading regressions return
  </acceptance_criteria>
  <done>Community-review-sensitive package rules are now checked automatically.</done>
</task>

</tasks>

<verification>
- `npm --prefix apps/obsidian run build` exits 0
- `node --test tests/obsidian-community-review.test.cjs` exits 0
- `rg -n "Open THRUNT workspace|createEl\\('h2'" apps/obsidian/src/main.ts apps/obsidian/src/settings.ts` returns no matches
</verification>

<success_criteria>
The Obsidian package satisfies the concrete package-level review expectations already identified in context and keeps those rules covered by a focused regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/67-community-directory-submission-readiness/67-01-SUMMARY.md`
</output>
