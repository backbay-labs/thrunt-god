---
phase: 65-obsidian-cli-install-channel
plan: 03
type: execute
wave: 3
depends_on: ["65-01", "65-02"]
files_modified:
  - bin/install.js
  - tests/hunt-install.test.cjs
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - INST-05
  - INST-06
must_haves:
  truths:
    - "Installer test coverage exercises vault discovery, staged bundle linking, reinstall repair, and no-vault fallback without relying on a real Obsidian install"
    - "Obsidian installer helpers are injectable/exported enough to let node:test use temp directories and mocked build behavior"
    - "`tests/hunt-install.test.cjs` proves the staged asset contract remains `main.js`, `manifest.json`, `styles.css`"
    - "Disposable CLI smoke coverage exercises the real `node bin/install.js --obsidian` operator path for help, invalid-flag rejection, install, reinstall, and no-vault fallback"
    - "Targeted installer tests run green with `node --test tests/hunt-install.test.cjs`"
  artifacts:
    - path: "tests/hunt-install.test.cjs"
      provides: "Installer regression coverage for the new Obsidian install mode"
      contains: "obsidian"
    - path: "bin/install.js"
      provides: "Test-exported helpers or injection points needed by the new test coverage"
      exports: ["installObsidian", "discoverObsidianVaults", "linkObsidianBundleIntoVault"]
  key_links:
    - from: "tests/hunt-install.test.cjs"
      to: "bin/install.js"
      via: "THRUNT_TEST_MODE helper exports"
      pattern: "THRUNT_TEST_MODE"
---

<objective>
Add automated regression coverage for the new Obsidian install flow and prove the mode is safe to rerun.

Purpose: This plan closes the phase by turning the new install mode into a tested contract. After it lands, the repo has direct tests for staging, vault discovery, symlink repair, and no-vault fallback behavior.

Output: Updated `tests/hunt-install.test.cjs` (and any minimal `bin/install.js` helper injection needed for testability) plus passing targeted installer tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-obsidian-cli-install-channel/65-CONTEXT.md
@bin/install.js
@tests/hunt-install.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the Obsidian installer helpers directly testable with temp directories and mocked build behavior</name>
  <files>bin/install.js</files>
  <read_first>
    - bin/install.js
    - tests/hunt-install.test.cjs
    - .planning/phases/65-obsidian-cli-install-channel/65-CONTEXT.md
  </read_first>
  <action>
Adjust `bin/install.js` only as needed for deterministic node:test coverage:

1. Ensure the new Obsidian helpers from Plans 01-02 accept injectable options instead of hard-coding environment state:
   - `homeDir`
   - `configPath`
   - `repoRoot`
   - `pluginDir`
   - `runBuild`
   - `logger` or equivalent output collector
2. Add CLI-safe environment overrides so spawned `node bin/install.js --obsidian` smoke tests can run against disposable fixtures:
   - `THRUNT_HOME` overrides the default home directory used for `~/.thrunt/obsidian`
   - `THRUNT_OBSIDIAN_CONFIG` overrides the default `obsidian.json` path
   - `THRUNT_OBSIDIAN_PLUGIN_SOURCE` overrides the default `apps/obsidian/` source directory for staged assets
   - `THRUNT_OBSIDIAN_SKIP_BUILD=1` skips `npm run build:obsidian` when fixture assets are already present
3. Keep those environment overrides optional and production-safe; when unset, production defaults must remain unchanged.
4. Ensure `installObsidian(options)` can be called directly from tests without parsing CLI flags and without mutating the real user home directory.
5. Keep the `THRUNT_TEST_MODE` export surface stable so tests can require the helpers directly.
  </action>
  <verify>
    <automated>node -e "process.env.THRUNT_TEST_MODE='1'; const m=require('./bin/install.js'); if(typeof m.installObsidian!=='function') process.exit(1); if(typeof m.discoverObsidianVaults!=='function') process.exit(1); if(typeof m.linkObsidianBundleIntoVault!=='function') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `installObsidian(options)` can run against temp directories by overriding home/config/plugin/build inputs
    - the CLI path can also be redirected into temp fixtures via `THRUNT_HOME`, `THRUNT_OBSIDIAN_CONFIG`, `THRUNT_OBSIDIAN_PLUGIN_SOURCE`, and `THRUNT_OBSIDIAN_SKIP_BUILD`
    - production defaults still use real `os.homedir()`, repo root, and build command when overrides are absent
    - THRUNT test exports still expose the Obsidian helpers after the refactor
  </acceptance_criteria>
  <done>The Obsidian install flow is testable both through direct helper calls and through disposable real-CLI invocations without touching the user's real Obsidian config or vaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add installer regression tests for staging, vault discovery, symlink repair, and no-vault fallback</name>
  <files>tests/hunt-install.test.cjs, bin/install.js</files>
  <read_first>
    - tests/hunt-install.test.cjs
    - bin/install.js
    - .planning/phases/65-obsidian-cli-install-channel/65-CONTEXT.md
  </read_first>
  <action>
Extend `tests/hunt-install.test.cjs` with Obsidian install coverage using temp directories:

1. Add a test for `discoverObsidianVaults()` that writes a temp `obsidian.json` in the real observed format:
```json
{"vaults":{"id-1":{"path":"/tmp/vault-a","ts":1,"open":true},"id-2":{"path":"/tmp/vault-b","ts":2}}}
```
   - create both vault directories
   - assert the helper returns the two absolute paths in order
2. Add a test for `linkObsidianBundleIntoVault()`:
   - create a temp staged bundle dir containing `main.js`, `manifest.json`, `styles.css`
   - create a temp vault dir
   - call the helper
   - assert each target under `.obsidian/plugins/thrunt-god/` exists and `lstatSync(...).isSymbolicLink()` is true
3. Add a reinstall/repair test:
   - create stale regular files or broken symlinks at the target paths
   - call `linkObsidianBundleIntoVault()` again
   - assert the stale targets were replaced by working symlinks to the staged bundle
4. Add a no-vault fallback test for `installObsidian(options)`:
   - point `configPath` at a missing file
   - provide a temp repo/plugin dir with staged source assets and a mocked `runBuild`
   - capture logger output
   - assert no `.obsidian/plugins/thrunt-god/` directory is created anywhere and output mentions manual install fallback
5. Add a happy-path test for `installObsidian(options)`:
   - create a temp `obsidian.json`, temp vault, temp `apps/obsidian` asset dir, and mocked `runBuild`
   - call the helper
   - assert the staged dir contains `main.js`, `manifest.json`, `styles.css`
   - assert the vault plugin dir contains symlinks to the staged assets
   - assert output includes `Restart Obsidian and enable THRUNT God in Community Plugins.`
6. Run the targeted test file:
   - `node --test tests/hunt-install.test.cjs`
  </action>
  <verify>
    <automated>node --test tests/hunt-install.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/hunt-install.test.cjs` contains coverage for vault discovery, symlink creation, reinstall repair, no-vault fallback, and successful install
    - happy-path tests verify the staged asset set is exactly `main.js`, `manifest.json`, and `styles.css`
    - no-vault tests assert no vault plugin directory is created and fallback text is emitted
    - `node --test tests/hunt-install.test.cjs` exits 0
  </acceptance_criteria>
  <done>Installer regression coverage proves the new Obsidian mode stages assets, discovers vaults, repairs links, and falls back safely.</done>
</task>

<task type="auto">
  <name>Task 3: Add real CLI smoke verification for help, invalid flags, install, reinstall, and no-vault fallback</name>
  <files>tests/hunt-install.test.cjs, bin/install.js</files>
  <read_first>
    - tests/hunt-install.test.cjs
    - bin/install.js
    - .planning/ROADMAP.md
    - .planning/phases/65-obsidian-cli-install-channel/65-CONTEXT.md
  </read_first>
  <action>
Extend `tests/hunt-install.test.cjs` with child-process smoke tests that exercise the real CLI entrypoint against disposable fixtures:

1. Add helper fixtures that create:
   - temp `THRUNT_HOME`
   - temp `obsidian.json`
   - temp vault directories
   - temp plugin source directory containing `main.js`, `manifest.json`, `styles.css`
2. Add a `--help` smoke test:
   - run `node bin/install.js --help`
   - assert output contains `--obsidian`
3. Add an invalid-combination smoke test:
   - run `node bin/install.js --obsidian --claude`
   - assert exit code is non-zero and stderr/stdout explains the invalid flag combination
4. Add a clean-install CLI smoke test using env overrides:
   - `THRUNT_HOME=<temp home>`
   - `THRUNT_OBSIDIAN_CONFIG=<temp obsidian.json>`
   - `THRUNT_OBSIDIAN_PLUGIN_SOURCE=<temp plugin source>`
   - `THRUNT_OBSIDIAN_SKIP_BUILD=1`
   - run `node bin/install.js --obsidian`
   - assert the staged dir under `<temp home>/.thrunt/obsidian/` contains the three assets
   - assert the detected vault contains symlinks to the staged assets
   - assert output includes `installed` and `Restart Obsidian and enable THRUNT God in Community Plugins.`
5. Add a reinstall CLI smoke test:
   - run the same command twice against the same temp fixture
   - assert the second run does not duplicate files and emits either `installed` or `skipped` for the vault
6. Add a no-vault CLI smoke test:
   - point `THRUNT_OBSIDIAN_CONFIG` at a missing or empty fixture
   - run `node bin/install.js --obsidian`
   - assert output mentions no vaults detected plus the manual-install fallback
7. Keep all smoke coverage in `tests/hunt-install.test.cjs` and re-run:
   - `node --test tests/hunt-install.test.cjs`
  </action>
  <verify>
    <automated>node --test tests/hunt-install.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/hunt-install.test.cjs` contains real-CLI smoke coverage for `--help`, invalid-flag rejection, clean install, reinstall, and no-vault fallback
    - CLI smoke tests use disposable env overrides instead of the real user home directory
    - clean-install smoke tests assert vault output includes `installed`
    - reinstall smoke tests assert the second run stays idempotent and reports `installed` or `skipped`
    - no-vault smoke tests assert output mentions no vaults detected and manual-install fallback guidance
    - `node --test tests/hunt-install.test.cjs` exits 0 after the smoke coverage is added
  </acceptance_criteria>
  <done>Real CLI smoke verification covers the operator-facing `--obsidian` flow in disposable fixtures, closing the roadmap's verification gap.</done>
</task>

</tasks>

<verification>
- `node --test tests/hunt-install.test.cjs` exits 0
- `grep -n 'obsidian' tests/hunt-install.test.cjs` shows the new Obsidian install coverage
- staged-asset assertions in the tests explicitly name `main.js`, `manifest.json`, and `styles.css`
- real-CLI smoke assertions cover `--help`, invalid-flag rejection, install, reinstall, and no-vault fallback
</verification>

<success_criteria>
Phase 65 finishes with automated regression coverage for the new Obsidian installer mode, including helper-level tests plus disposable real-CLI smoke verification for install, reinstall, invalid flags, and no-vault fallback behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/65-obsidian-cli-install-channel/65-03-SUMMARY.md`
</output>
