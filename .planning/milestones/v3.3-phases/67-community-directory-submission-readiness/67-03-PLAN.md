---
phase: 67-community-directory-submission-readiness
plan: 03
type: execute
wave: 3
depends_on: ["67-01", "67-02"]
files_modified:
  - .gitignore
  - manifest.json
  - versions.json
  - package.json
  - scripts/sync-obsidian-submission-files.cjs
  - docs/obsidian-community-submission.md
  - docs/obsidian-community-plugin-entry.json
  - tests/obsidian-community-submission.test.cjs
autonomous: true
requirements:
  - COMM-03
  - COMM-04
must_haves:
  truths:
    - "The repository root contains submission-facing `manifest.json` and `versions.json` mirrors that stay synchronized with `apps/obsidian/` through one scripted maintainer flow"
    - "The repo includes a concrete, reusable PR-entry snippet and checklist for updating `obsidianmd/obsidian-releases`"
    - "Submission metadata and handoff behavior are covered by an automated sync test instead of tribal knowledge"
    - "Release bundle outputs stay out of git status noise through ignore rules"
  artifacts:
    - path: "scripts/sync-obsidian-submission-files.cjs"
      provides: "One-step synchronization of repository-root submission metadata from the app package"
    - path: "manifest.json"
      provides: "Repository-root submission manifest for community-plugin ingestion"
    - path: "versions.json"
      provides: "Repository-root compatibility map for community-plugin ingestion"
    - path: "docs/obsidian-community-submission.md"
      provides: "Maintainer checklist and release-to-directory handoff notes"
    - path: "docs/obsidian-community-plugin-entry.json"
      provides: "Ready-to-paste `community-plugins.json` entry snippet"
    - path: "tests/obsidian-community-submission.test.cjs"
      provides: "Automated verification that root submission metadata stays synchronized"
  key_links:
    - from: "scripts/sync-obsidian-submission-files.cjs"
      to: "apps/obsidian/manifest.json"
      via: "source metadata sync"
      pattern: "manifest.json"
    - from: "tests/obsidian-community-submission.test.cjs"
      to: "manifest.json"
      via: "root-to-app submission metadata parity checks"
      pattern: "manifest"
    - from: "docs/obsidian-community-submission.md"
      to: "docs/obsidian-community-plugin-entry.json"
      via: "documented PR handoff snippet"
      pattern: "community-plugin-entry"
---

<objective>
Add the submission metadata, checklist, and documented handoff flow needed to keep community-directory updates synchronized with each Obsidian release.

Purpose: The release pipeline from Phase 66 is not enough by itself because the community directory also reads repository-root metadata and requires a manual PR step. After this plan lands, the repo has root submission metadata, a sync script, a checklist/runbook, and a ready-to-use PR entry snippet.

Output: Repository-root submission metadata, a sync script/package entrypoint, a submission runbook, a PR entry snippet, and a sync regression test.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-community-directory-submission-readiness/67-CONTEXT.md
@package.json
@apps/obsidian/package.json
@apps/obsidian/manifest.json
@apps/obsidian/versions.json
@docs/obsidian-release.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root-level submission metadata and a scripted sync path for Obsidian community-plugin files</name>
  <files>.gitignore, manifest.json, versions.json, package.json, scripts/sync-obsidian-submission-files.cjs</files>
  <read_first>
    - package.json
    - apps/obsidian/package.json
    - apps/obsidian/manifest.json
    - apps/obsidian/versions.json
    - .gitignore
  </read_first>
  <action>
Create one concrete maintainer path for submission metadata sync:

1. Add `scripts/sync-obsidian-submission-files.cjs` that copies `apps/obsidian/manifest.json` and `apps/obsidian/versions.json` into the repository root and prints a JSON payload describing the synced files.
2. Add a root package script such as `"sync:obsidian-submission": "node scripts/sync-obsidian-submission-files.cjs"`.
3. Commit repository-root `manifest.json` and `versions.json` as the current synced submission metadata.
4. Update `.gitignore` so `dist/` release bundle output stays untracked.
  </action>
  <verify>
    <automated>node scripts/sync-obsidian-submission-files.cjs</automated>
  </verify>
  <acceptance_criteria>
    - the repo root contains `manifest.json` and `versions.json`
    - `package.json` exposes a sync script for root submission metadata
    - `node scripts/sync-obsidian-submission-files.cjs` exits 0 and keeps root metadata synchronized with `apps/obsidian/`
    - `.gitignore` ignores `dist/`
  </acceptance_criteria>
  <done>The repo has one scripted path for keeping submission-facing root metadata in sync with the real Obsidian package metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Write the submission checklist and ready-to-paste community-plugin entry metadata</name>
  <files>docs/obsidian-community-submission.md, docs/obsidian-community-plugin-entry.json</files>
  <read_first>
    - docs/obsidian-release.md
    - README.md
    - manifest.json
    - versions.json
  </read_first>
  <action>
Create the maintainer-facing submission handoff docs:

1. Add `docs/obsidian-community-submission.md` documenting:
   - the local release + metadata sync flow
   - required repo-root files
   - required GitHub release assets
   - the exact `obsidianmd/obsidian-releases` PR/update checklist
2. Add `docs/obsidian-community-plugin-entry.json` containing the ready-to-paste object for `community-plugins.json` with the correct `id`, `name`, `author`, `description`, and `repo`.
3. Make the runbook explicitly reference the sync script and the Phase 66 release bundle command so the two flows stay unified.
  </action>
  <verify>
    <automated>rg -n "sync:obsidian-submission|bundle:obsidian-release|community-plugins.json|manifest.json|versions.json" docs/obsidian-community-submission.md && test -f docs/obsidian-community-plugin-entry.json</automated>
  </verify>
  <acceptance_criteria>
    - `docs/obsidian-community-submission.md` documents the full release-to-directory handoff
    - `docs/obsidian-community-plugin-entry.json` exists and contains the current plugin metadata snippet
    - the runbook references both the sync script and release bundle command
  </acceptance_criteria>
  <done>The repo contains the checklist and PR metadata needed to submit or update the community-plugin entry without guesswork.</done>
</task>

<task type="auto">
  <name>Task 3: Add a submission-metadata regression test that proves root metadata stays synchronized</name>
  <files>tests/obsidian-community-submission.test.cjs</files>
  <read_first>
    - scripts/sync-obsidian-submission-files.cjs
    - manifest.json
    - versions.json
    - apps/obsidian/manifest.json
    - apps/obsidian/versions.json
  </read_first>
  <action>
Create `tests/obsidian-community-submission.test.cjs` to enforce the submission handoff contract:

1. Run `node scripts/sync-obsidian-submission-files.cjs`.
2. Assert the repository-root `manifest.json` exactly matches `apps/obsidian/manifest.json`.
3. Assert the repository-root `versions.json` exactly matches `apps/obsidian/versions.json`.
4. Assert the package script `sync:obsidian-submission` exists.
5. Assert the PR entry JSON snippet matches the current plugin metadata values.
  </action>
  <verify>
    <automated>node --test tests/obsidian-community-submission.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/obsidian-community-submission.test.cjs` exists and enforces root/app metadata parity
    - `node --test tests/obsidian-community-submission.test.cjs` exits 0
    - the test fails if root submission metadata drifts from the app metadata or the PR snippet becomes stale
  </acceptance_criteria>
  <done>The submission handoff path is now verifiable and resistant to silent metadata drift.</done>
</task>

</tasks>

<verification>
- `node scripts/sync-obsidian-submission-files.cjs` exits 0
- `node --test tests/obsidian-community-submission.test.cjs` exits 0
- `test -f manifest.json && test -f versions.json && rg -n "dist/" .gitignore`
</verification>

<success_criteria>
The repo contains synchronized root submission metadata plus the checklist, snippet, and automated safeguards needed to maintain the Obsidian community-plugin entry over time.
</success_criteria>

<output>
After completion, create `.planning/phases/67-community-directory-submission-readiness/67-03-SUMMARY.md`
</output>
